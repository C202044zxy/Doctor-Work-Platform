
## 2. 模块总览

### 2.1 模块清单

| 模块 | 名称 | 性质 | 依赖 | 量级 | 主要交付 |
|------|------|------|------|------|---------|
| **M0** （已完成）| 基建与契约 | 必做 | — | 14h | 脚手架、建库、配置、一键启动、契约同步 |
| **M1** （已完成）| 安全账户登录 | 🔴 必做·前置 | M0 | 30h | 2FA 登录、三渠道、JWT、RBAC、数据范围、临时授权 |
| **M2** （已完成）| 患者信息管理 | 必做 | M0、M1 | 25h | 患者 CRUD、过敏警示、病史、分组、详情页 |
| **M3** (李奕儒)| 在线问诊 | 必做 | M1、M2 | 30h | WebSocket、图文/图片、三态工作台、记录检索、视频通话 |
| **M4** (周湘予)| 电子病历 EMR | 🔴 必做·最高分 | M1、M2 | 38h | 模板、病历版本、医嘱、**校验引擎**、审阅归档 |
| **M5** （陆璟）| 远程会诊 | 必做 | M1、M2、M3 | 12h | 状态机、自动授权联动、资料共享、报告 |
| **M6** （葛航）| 患者健康管理 | 必做 | M1、M2 | 22h | 体征、阈值、趋势图、提醒、评估 |
| **M7** | 医生社区 | ⛔ 仍不做 | — | — | 仅保留答辩口径（§3.8） |
| **M8** （已完成）| 操作日志与审计 | 🔴 必做·横切 | M0、M1 | 8h | 中间件埋点、敏感读埋点、查询与 CSV 导出 |
| **M9** | 改造与横切收口 | 🔴 必做 | 贯穿 | 16h | 响应/分页/角色名统一、脱敏规范、前端三态 |
| **S1** （已完成）| 触达通道模拟 | 模拟·必做 | M1、M6 | 8h | 模拟短信网关 + 模拟微信触达 |
| **S2** （已完成）| 人脸识别模拟 | 模拟·必做 | M1 | 6h | 录入/比对/登录渠道 + 演示模态 |
| M3**S3** | 会话录像模拟 | 模拟 | M3 | 8h | 通话时间线重建 + 模拟回放页 |
| M6**S4** | IoT 体征采集模拟 | 模拟 | M6 | 10h | 设备模型 + 上报接口 + 设备模拟器 + 设备页 |
| M0 + M8**S5** | 企业级监控面板模拟 | 模拟 | M0、M8 | 12h | 自采指标 + `/metrics` + 监控页 + 简易告警 |
| M1 **S6** | 动态权限配置模拟 | 模拟 | M1 | 10h | 权限落库 + 配置矩阵页 + 防自锁 |
| **D01–D07** (左宸睿)| 文档与验收 | 必做 | 各模块 | 50h | 规格、原型、设计文档、演示脚本、UAT |
week1内容校验（杨文桐）
## 3. 模块任务与测试场景（M0–M9）

> 每个模块统一格式：**目标 → 交付内容 → 完成标准 → 测试场景**。
> 测试场景中的账号与数据统一引用 §6.2 的演示基线。

### 3.1 M0 · 基建与契约 ｜ 必做 ｜ 依赖：无 ｜ 14h

**目标**：新机器一条命令拉起全栈；契约（`openapi.yaml`）成为前后端唯一事实源。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M0-01 | 脚手架（FastAPI 分包 + Vue3 + 分支规范） | 可运行骨架 | T01 ✅ |
| M0-02 | 建库建表 + Alembic 迁移（全量业务表） | 迁移脚本、seed | T02 🟡 |
| M0-03 | Redis 接入 + 配置管理 + 统一响应/异常封装 | `get_redis()`、异常处理器 | T03 🟡 |
| M0-04 | 一键启动脚本 + README + 部署说明 | `dev.{sh,ps1}` | T04 ✅ |
| M0-05 | **契约同步**：`openapi.yaml` 与已实现接口逐字段对齐；补 CI 校验 | 契约校验脚本 | 新增 |

**完成标准**

- [ ] 空库执行 `alembic upgrade head` 一次成功，`downgrade base` 可回滚且幂等。
- [ ] `GET /health` 与 `GET /api/health` **响应逐字节相同**；**Redis 断连仍返回 200**，body 标 `redis:"down"`。
- [ ] 全部接口返回 `{code, message, data}`；分页统一 `page`/`size` → `{items,total,page,size}`。
- [ ] `audit_logs` 表对业务账号**无 UPDATE/DELETE 权限**（SQL 脚本随迁移交付）。
- [ ] CI 增加一步：用 `openapi.yaml` 校验实现的路由集合（缺接口 / 多接口都要报错）。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M0-T1 | 正向 | 全新机器 | 克隆 → 一条命令启动 | 10 分钟内 `/docs` 与前端首页都可打开 |
| M0-T2 | 反向 | 删掉 `.env` 的 `JWT_SECRET` | 启动后端 | **立即抛配置错误并退出**，不是运行时才崩 |
| M0-T3 | 边界 | Redis 运行中 | `docker stop redis` 后再调 `/health` | 仍 200，`redis:"down"`，登录以外接口不受影响 |
| M0-T4 | 边界 | 无 docker 的机器 | 执行 `dev.sh docker` | 打印安装指引 + 非 0 退出码，不静默卡死 |
| M0-T5 | 契约 | — | 跑契约校验脚本 | 路由集合与 `openapi.yaml` 完全一致 |
| M0-T6 | 安全 | 业务账号 | `UPDATE audit_logs …` / `DELETE FROM audit_logs` | **两者均被拒绝** |

---

### 3.2 M1 · 安全账户登录 ｜ 🔴 必做·全平台前置 ｜ 依赖：M0 ｜ 30h

**目标**：三种 2FA 渠道（邮箱 / 短信 / 人脸）任选其一完成登录；三个角色的功能权限与科室数据范围在所有模块上真实生效。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M1-01 | 用户/角色/部门模型 + bcrypt + JWT 签发校验 | `POST /api/auth/login`、`/verify-code`、`/logout`、`GET /api/me` | T05 |
| M1-02 | 验证码服务（渠道无关）：签发 / 校验 / TTL / 一次性 / 限流 | `verification` 服务 | T06 |
| M1-03 | 登录页 + 验证码页 + Token 存储 + 路由守卫 | 前端登录闭环 | T07 |
| M1-04 | RBAC 权限矩阵 + 接口鉴权（**声明式，禁止散落 `if role == …`**） | 权限依赖注入 | T08 |
| M1-05 | 科室数据范围过滤（service 层统一入口，admin 豁免） | 过滤中间层 | T09 |
| M1-06 | 临时授权 `temp_grant` + APScheduler 到期回收 | 授权 CRUD + 定时任务 | T10 |
| M1-07 | 用户管理 CRUD + 角色/科室字典 + 前端用户管理页 | `/api/users`、`/api/roles` | 原文件未判定归属，**本版明确纳入 M1** |
| M1-08 | 登录失败限速（密码与人脸/短信失败**共用同一计数器**） | 429 + 剩余秒数 | T05 S2 延伸 |

**完成标准**

- [ ] 权限矩阵**声明式配置**；越权返回 403 且 `message` 指明缺哪条权限。
- [ ] 权限失败**两套口径不可混用**：角色功能不足 → 403；跨科室数据 → **404**。
- [ ] 匿名白名单显式声明，且**必须包含 `send-code`**（旧签收标准 T08 §4 漏了它，本版更正）。
- [ ] 三种 401（无 token / 过期 / 黑名单）`message` 可区分；`status=disabled` 持有效 token 亦被拒。
- [ ] `temp_grant` 表字段严格为 `(grantee_id, patient_id, reason, expire_at, is_valid)`，**不引入 `expires_at` / `status`**。
- [ ] 提供 3 角色 × 5 资源 = 15 组合的**期望结果表**，可脚本化回归。
- [ ] 提供匿名白名单、403/404 口径的自动化断言。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M1-T1 | 正向 | `admin_zhang` | 密码 → 收码 → 提交验证码 | 返回 JWT；`GET /api/me` 返回 `title=admin`、`department=信息科` |
| M1-T2 | 正向 | 15 组合矩阵 | 逐格调用 | 15/15 与期望表一致 |
| M1-T3 | 反向 | `dr_wang`(junior) | 调 `POST /api/emr/records/{id}/review` | **403**，指明缺 `emr.review` |
| M1-T4 | 反向 | `dr_wang` | 调 `GET /api/patients/P20260003`（神经内科） | **404**（不是 403；返回 403 即泄露存在性） |
| M1-T5 | 反向 | 已登录 | 立即 logout → 用同一 JWT 再调 | 401 且 message 为"凭证已失效" |
| M1-T6 | 反向 | `dr_wang` | 错误密码连续 5 次 | 第 5 次起返回 429 / 锁定提示，带剩余秒数 |
| M1-T7 | 边界 | `dr_chen` | 被授权访问 `P20260001` → 手改 `expire_at` 为 1 分钟后 → 等 90 秒 | 授权期内可访问；到期后**回落 404**，无需重启 |
| M1-T8 | 正向 | 授权动作 | 查审计 | 存在 `temp_grant.create` 与 `temp_grant.expire` 两条 |
| M1-T9 | 边界 | 库中改 `dr_wang` 科室为神经内科 → 重新登录 | 查患者列表 | 可见集合随之变为 `P20260003`（证明动态读，非硬编码） |
| M1-T10 | 安全 | 地址栏直输 `/audit` | 以 `dr_wang` 登录后访问 | 403 无权限页；**不是白屏、菜单里也没有入口** |

---

### 3.3 M2 · 患者信息管理 ｜ 必做 ｜ 依赖：M0、M1 ｜ 25h

**目标**：患者档案是全平台数据核心，M3–M6 全部围绕它展开；过敏数据是 M4-04 校验引擎的输入。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M2-01 | 患者 CRUD + 四条件组合搜索 + 患者号生成 + 加密脱敏 | `/api/patients*` | T13 ✅ |
| M2-02 | 过敏记录管理 + 过敏原字典 + 红色警示条数据源 | `/api/patients/{no}/allergies`、`/api/allergens` | T14 🟡 |
| M2-03 | 患者列表页（搜索 + 分页 + 三态 + 脱敏展示） | `PatientListView` | T15 🟡 |
| M2-04 | 患者详情页（基本信息 / 病史时间线 / 过敏警示条 / 治疗方案 Tab） | `PatientDetailView` | T16 |
| M2-05 | 患者分组（创建/重命名/删除 + 批量加成员 + 按组筛选） | `/api/patient-groups*` | T17 |
| M2-06 | 病史 CRUD（时间线数据源，`symptom_tags` 结构化以支持搜索） | `/api/patients/{no}/histories` | T14 拆分 |

**完成标准**

- [ ] 四条件（姓名模糊 / `patient_no` 精确 / `symptom_tags` / 入院日期区间）可**任意 AND 组合**，全空返回全量分页。
- [ ] `patient_no` **系统生成**，请求体不接受该字段；前端新建表单无此输入框。
- [ ] 手机号 / 身份证 AES-256-GCM 加密入库，列表返回脱敏值；`has_severe_allergy` 由后端算，不由前端遍历。
- [ ] 提供标准函数 `get_patient_allergens(patient_no)`，**详情接口与 M4-04 校验引擎共用同一数据源**。
- [ ] 过敏增/删/改均写审计（含被删过敏原名称）；删除患者时过敏级联策略写入接口文档。
- [ ] 前端列表页**无 demo 数据残留**；空 / 加载 / 错误三态齐全。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M2-T1 | 正向 | — | 搜"赵" + 症状标签"胸痛" + 日期区间 | 命中 `P20260001`，`total = 1` |
| M2-T2 | 正向 | `P20260001` | 添加"青霉素类/严重""磺胺类/中度" | 详情返回 2 条；列表页出现红色角标；详情顶部红色警示条 |
| M2-T3 | 反向 | — | 新建患者不填姓名 | 422 且 message 指明 `name` |
| M2-T4 | 反向 | — | 上传 `../evil.png` 类的患者号 / 非法 `patient_no` 字段 | 422；`patient_no` 被忽略而非写入 |
| M2-T5 | 安全 | — | 查库核对 | `phone_enc` 为密文；接口响应为 `138****1234` |
| M2-T6 | 反向 | 停掉后端 | 打开列表页 | 显示"加载失败，点击重试"，**控制台无未捕获异常**；白屏或无限 loading 即不通过 |
| M2-T7 | 边界 | 500 条患者 | 翻页 | 单页响应 < 1.5s；翻到第 2 页再搜索**自动回第 1 页** |
| M2-T8 | 反向 | — | 删除"磺胺类"过敏 | 警示条只剩青霉素类；审计存在 `allergy.delete` 且含被删过敏原名 |
| M2-T9 | 正向 | 分组 | 创建"慢病随访" → 批量加入 2 名患者 → 按组筛选 | 列表精确返回 2 名；重复加入同一患者 → 409 |

---

### 3.4 M3 · 在线问诊 ｜ 必做 ｜ 依赖：M1、M2 ｜ 30h

**目标**：图文问诊做实；视频通话走浏览器原生 WebRTC，不接第三方服务。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M3-01 | WebSocket 服务 + 会话/消息模型 + 实时收发（JWT 握手校验） | `/ws/chat/{room_id}` | T25 |
| M3-02 | 图文消息 + 图片存储（白名单 / 大小 / 重命名 / StaticFiles） | `/api/consultations/{id}/messages`、`/api/uploads/images` | T26 |
| M3-03 | 问诊工作台（待接诊/进行中/已结束）+ 聊天室 UI + 发送中/已送达态 | `ConsultationsView` | T27 |
| M3-04 | 问诊记录中心（患者/日期/关键词）+ CSV 导出 | `/api/consultations/records`、`/export` | T28 |
| M3-05 | 视频通话（WebRTC 点对点 + WS 信令 + 通话元数据） | `/api/consultations/{id}/calls` | T29 |
| M3-06 | 会话状态机（`waiting → active → ended`）+ 结束后只读 | `/accept`、`/end` | T27 拆分 |

**完成标准**

- [ ] WebSocket 路径为 **`/ws/chat/{room_id}`**，问诊与会诊**复用同一路由**；连接时校验 JWT，无效 token 拒绝握手。
- [ ] 广播的 `data` **必须带持久化后的 `id`**（否则前端做不出"发送中/已送达"两态）。
- [ ] 信令房间**校验参与者**：JWT 有效但不在会话名单上的人，在升级完成前拿到 **403**。
- [ ] 图片四条硬约束：内容类型白名单（jpg/png/**webp**）、≤ 5MB、文件名随机重命名、`uploads/` 不进 Git。
- [ ] 断线重连**不丢消息**（按最后消息时间补齐）；异常断开清理在线状态，不残留"幽灵在线"。
- [ ] 视频：同机双标签页或同局域网两台机器可完成一次完整通话；`call_log` 记录起止与时长。
- [ ] 跨公网需 TURN 的局限**写入文档并进入答辩口径**。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M3-T1 | 正向 | 两个浏览器标签 | 同房间 A 发"你好" | B 在 1 秒内收到；刷新 B 后历史消息仍在 |
| M3-T2 | 反向 | — | 不带 token 连 `ws://…/ws/chat/1` | **握手被拒**；JWT 有效但非参与者 → 403 |
| M3-T3 | 边界 | 已连接 | 断网 10 秒后恢复 | 期间对方消息在重连后**补齐显示**，不丢失 |
| M3-T4 | 正向 | 三态流转 | 新请求 → 接诊 → 发消息 → 结束 | 依次落到 待接诊 / 进行中 / 已结束；结束后输入框禁用且 `ended_at` 有值 |
| M3-T5 | 边界 | 含 50 条历史消息的会话 | 打开 → 上滚加载 | 顺序正确、无重复、无遗漏；图文相对顺序不变 |
| M3-T6 | 反向 | — | 上传 `.exe` 改名 `.jpg` / 20MB 图片 / `../evil.png` | 分别被内容类型、大小、重命名规则拒绝；落盘文件未逃逸 `uploads/` |
| M3-T7 | 正向 | 同局域网两台机器 | `dr_wang` 发起视频 → `dr_li` 接听 → 通话 60 秒 → 挂断 | 双向音视频可见可闻；问诊记录新增通话记录，时长 ≈ 60s（±2s） |
| M3-T8 | 反向 | 视频呼叫中 | `dr_li` 拒接 / 直接关浏览器 | 30 秒内 `dr_wang` 端回落图文态；`call_log` 无幽灵"进行中"记录 |
| M3-T9 | 正向 | 有历史会话 | 筛选"赵大勇 + 本月 + 关键词'胸痛'" → 导出 CSV | 命中 1 条；Excel 打开无乱码，行数与页面一致 |
| M3-T10 | 安全 | `dr_wang` | 检索"孙建国"（神经内科） | **0 条**；`admin_zhang` 检索同一关键词有结果 |
| M3-T11 | 性能 | 单房间 20 并发连接 | 群发消息 | 消息延迟 < 500ms（本地/局域网自测，记录实测值） |

---

### 3.5 M4 · 电子病历 EMR ｜ 🔴 最高分模块 ｜ 依赖：M1、M2 ｜ 38h

**目标**：模板书写 → 医嘱校验（过敏拦截 / 剂量警告）→ 提交 → 上级审阅 → 归档只读，全链路可演示。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M4-01 | 模板模型 + 2 个预置模板（「入院记录」「日常病程记录」）+ 模板管理接口 | `/api/emr/templates*` | T18 |
| M4-02 | 病历 CRUD + 版本管理（草稿/提交/全量快照/补记） | `/api/emr/records*` | T19 |
| M4-03 | 医嘱模型 + 开立/修改/停用（软停） | `/api/emr/orders*` | T20 |
| M4-04 | ⭐ **医嘱校验引擎**（过敏红色拦截 + 剂量黄色警告，规则配置化） | `/api/emr/orders/validate`、`/api/drugs` | T21 |
| M4-05 | 病历编辑器前端（选模板 → 动态表单 → 暂存/提交） | `MedicalRecordView` | T22 |
| M4-06 | 医嘱前端 + 红/黄校验弹窗 + 列表校验标记 | `OrdersPanel` | T23 |
| M4-07 | 审阅流程（待审队列 / 通过归档 / 退回附意见）+ 归档只读锁定 + 我的提交 | `/api/emr/reviews`、`/review`、`/my-submissions` | T24 |

**完成标准**

- [ ] `fields_json` 只支持 **`text` / `number` / `date` / `select` / `textarea`** 五种；模板停用走 `is_active`，**停用后不影响已有病历渲染**。
- [ ] 状态机 `draft → pending → archived / rejected`；**`approve` 之后直接 `archived`，没有中间态**。
- [ ] 版本号每次提交 +1 并**存全量快照**；`PATCH` **不产生版本号**。
- [ ] 归档锁定返回 **409**（不是 400）；归档后修订只能走**补记**（新增关联版本，原文不变）。
- [ ] 🔴 `blocked` 命中过敏时**拒绝落库**，返回 **409**，文案带过敏原名；弹窗**没有"强制开立"出口**。
- [ ] 🟡 `warning` 剂量超限允许继续但需二次确认，文案带常规范围；两类同时命中**按严重度排序展示**。
- [ ] 校验规则**配置化**，且**在服务端执行**——绕过前端直接调 API 仍被拦截。
- [ ] 审计三条动作齐全：`medical_order.create` / `.modify` / `.stop`（**旧契约漏了 `create`**，本版明确要求）。
- [ ] `drug` 项缺 `dose` → 422 且指明字段（`lab`/`exam` 项不受此约束）。
- [ ] 提交失败**不得丢失已输入内容**（前端本地暂存）。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M4-T1 | 正向 | `admin_zhang` | 打开「入院记录」 | 字段 ≥ 6，控件类型正确（日期→日期选择器、单选→下拉） |
| M4-T2 | 反向 | `dr_wang`(junior) | 调模板新增接口 | 403 |
| M4-T3 | 正向 | `dr_wang` | 建病历 → 暂存(v1,draft) → 提交(v2,pending) → 再改提交(v3) | 历史版本列表出现 v1/v2/v3，逐条内容正确 |
| M4-T4 | 反向 | 病历已 `archived` | 调修改接口 | **409 + "已归档，不可修改"**；前端编辑按钮消失、表单只读 |
| M4-T5 | 正向 | 归档病历 | 补记 | 生成 v4 且 v3 内容不变 |
| M4-T6 | **正向·六格矩阵** | 见下表 | 逐格实跑 | **6/6 全对才通过** |
| M4-T7 | 反向 | — | 用 curl **绕过前端**为 `P20260001` 开阿莫西林 | 仍返回 blocked 且**未落库**；审计有 `medical_order.blocked` |
| M4-T8 | 正向 | `P20260002`（无过敏） | 开呋塞米 20mg → 改 40mg → 停用 | `passed` → 修改成功 → `status=stopped` 但记录仍在 |
| M4-T9 | 反向 | — | 对不存在 `record_id` 开医嘱；`drug` 项不填剂量 | 404；422 且指明 `dose` |
| M4-T10 | 正向 | 审计 | 查 create / modify / stop | **三条记录齐全** |
| M4-T11 | 正向 | `dr_li`(senior) | 待审队列 → 通过 | 状态 `archived`，写入 `reviewer_id`/`reviewed_at` |
| M4-T12 | 反向 | `dr_wang` | 查看待审队列 | 不可见（403）；但**能看到自己的「我的提交」** |
| M4-T13 | 正向 | `dr_li` 退回并填意见 | 切回 `dr_wang` 查看 | "我的提交"显示已退回**且能看到退回意见** → 修改重提 → 版本 +1 → 重回待审队列 |
| M4-T14 | 反向 | 已停用医嘱 | 再点停用 | 按钮禁用或提示"已停用"；列表中 `blocked` 的记录**不应存在** |
| M4-T15 | 逆向 | 断网 | 点提交 → 恢复 → 重试 | 提示可重试；**已填内容一条不丢**；刷新后不丢是必测项 |
| M4-T16 | 边界 | 病历乐观锁 | 两个端同时保存同一 `version` | 后者 409，前端提示"病历已被他人修改，请刷新" |

**M4-T6 六格校验矩阵（逐格实跑，全对才签收）**

| # | 患者 | 药品与剂量 | 期望 |
|---|------|-----------|------|
| 1 | `P20260001`（青霉素类过敏） | 阿莫西林胶囊 0.5g tid | 🔴 blocked，提示青霉素类，**未落库** |
| 2 | `P20260001` | 复方新诺明 2 片 bid | 🔴 blocked，提示磺胺类，**未落库** |
| 3 | `P20260001` | 呋塞米片 80mg qd | 🟡 warning（常规 20–40mg），确认后落库 |
| 4 | `P20260001` | 呋塞米片 20mg qd | ✅ passed |
| 5 | `P20260001` | 阿莫西林 + 超剂量 | 🔴 blocked 优先展示，warning 一并列出 |
| 6 | `P20260002`（无过敏） | 阿莫西林胶囊 0.5g tid | ✅ passed |

---

### 3.6 M5 · 远程会诊 ｜ 必做 ｜ 依赖：M1、M2、M3 ｜ 12h

**目标**：会诊状态机 + **自动建临时授权**（流程 2 的设计亮点）+ 资料共享 + 报告归档到患者档案。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M5-01 | 会诊模型 + 状态机 + 邀请箱 + **发起时自动建 `temp_grant`** | `/api/meetings*` | T30 |
| M5-02 | 资料上传与共享（类型宽于图片：PDF / 图片 / 文档） | `/api/meetings/{id}/materials`、`/api/materials/{id}/download` | T31 |
| M5-03 | 报告生成（Jinja2 渲染 + 打印页）+ 列表/详情前端 | `/api/meetings/{id}/report` | T32 |

**完成标准**

- [ ] 状态机 `requested → accepted → in_progress → completed`，含 `declined` 分支；**没有 `archived`**（归档的是**报告**）。
- [ ] 非法跳转返回 **409**；非受邀人访问 → 403/404，不泄露存在性。
- [ ] **发起人可以是 `junior`**（`dr_wang` 发起是演示剧本的一部分）。
- [ ] `purpose`（目的）**必填且能读回**；`scheduled_at` **可选**，不传时后端取当前时刻。
- [ ] 发起时**自动为每位受邀专家建 `temp_grant`**，有效期 = 会诊时间 + 24h。
- [ ] `GET /api/meetings?patient_no=…` 必须可用——**"会诊记录"Tab 的取数入口**，缺它 T32 的归档步骤走不完。
- [ ] 上传**复用 M3-02 的上传组件，但白名单更宽**（PDF 必须能过）；下载响应带 `Content-Disposition`（含 RFC 5987 `filename*`），中文名不乱码。
- [ ] 报告生成后结论不可静默覆盖；如需修改走新版本。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M5-T1 | 正向 | `dr_wang` | 对 `P20260001` 发起会诊邀请 `dr_chen` | 建会诊成功；`temp_grant` 表**自动新增** grantee=`dr_chen`、patient=`P20260001`、expire ≈ 24h 后 |
| M5-T2 | 正向 | 上一步 | `dr_chen` 邀请箱 → 接受 | 状态 `accepted`；`dr_chen` 可查该患者详情（原为 404） |
| M5-T3 | 反向 | — | 把 `requested` 直接置 `completed`；发起时缺 `purpose` | 409；422 且指明 `purpose` |
| M5-T4 | 正向 | 会诊中 | `dr_wang` 上传 `心电图-2026-09-01.pdf` | `dr_chen` 立即可见 → 下载 → 文件正常打开、**中文名不乱码** |
| M5-T5 | 反向 | — | 用**非参与医生**账号打开下载 URL | **403**（服务端校验，不靠前端隐藏） |
| M5-T6 | 正向 | 会诊置完成 | 填 3 位专家意见 + 结论 → 生成报告 | 预览页完整；`Ctrl+P` 为 A4 且无裁切 |
| M5-T7 | 正向 | 报告已生成 | 打开 `P20260001` 患者详情"会诊记录"Tab | 能看到该报告（走 `?patient_no=` 取回） |
| M5-T8 | 安全 | 会诊结束 | 等 `temp_grant` 到期 → `dr_chen` 刷新 | 该患者回落 **404** |

---

### 3.7 M6 · 患者健康管理 ｜ 必做 ｜ 依赖：M1、M2 ｜ 22h

**目标**：方案 → 体征 → 趋势图 → 提醒 → 评估，流程 3 的完整闭环。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M6-01 | 体征模型 + 录入 + 阈值判定（bp/gl/hr） | `/api/patients/{no}/vitals*` | T33 |
| M6-02 | ⭐ ECharts 趋势图（多指标切换 + 超阈值标红 + 血压双值） | 趋势图组件 | T34 |
| M6-03 | 健康方案 CRUD + 前端（可关联提醒规则） | `/api/health-plans*` | T35 |
| M6-04 | 自动提醒（APScheduler + 工作台红点 + 未读数 + 幂等） | `/api/reminder-rules*`、`/api/reminders*` | T36 |
| M6-05 | 定期评估表单 + 历史查看 | `/api/patients/{no}/assessments` | T37 |

**完成标准**

- [ ] `sign_type` 取值**只有 `bp` / `gl` / `hr`**（没有 `weight`）；超阈值字段名是 **`is_abnormal`**（不是 `out_of_range`），由后端算。
- [ ] 趋势接口返回**时间序对**，可直接喂 ECharts；逐点 `threshold` 带**第二值**（`min_secondary`/`max_secondary`），否则血压 tooltip 显示不出 140/90。
- [ ] 区间内无数据 → `points: []`，前端显示**空态提示**（不是白屏、不是报错）。
- [ ] 阈值配置**集中管理**，不散落在业务代码中。
- [ ] 提醒：`PATCH /api/reminder-rules/{id}` **只发 `{"active": false}` 也必须是合法请求**（停用是单独动作）。
- [ ] 提醒生成有**幂等键（`rule_id + due_at`）**，重启不重复补发；APScheduler 任务随应用启动**只注册一次**。
- [ ] `POST /api/health-plans` 的 `new_reminder_rules` 支持**内联新建**规则（旧契约只收已存在 id，导致"创建方案时一并配置提醒"表达不出来）。
- [ ] 评估：改他人评估 → 403；不填结论 → 前端拦截**且后端也 422**。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M6-T1 | 正向 | `P20260001` | 录入血压 160/100；再录 120/80；心率 110；血糖 7.8 | 依次得到 `is_abnormal` = true / false / 按阈值 / 按阈值 |
| M6-T2 | 反向 | — | 录入血压 `"abc"` / 未来时间 | 422 且指明字段 |
| M6-T3 | 安全 | `dr_wang` | 查 `P20260003`（神经内科）体征 | **404** |
| M6-T4 | 正向·演示点 | 有 14 天血压数据 | 打开健康数据 → 默认近 7 天血压 → 悬停异常点 → 切血糖 → 切近 30 天 | 160/100 的点**标红**；tooltip 显示"160/100 mmHg（超出阈值 140/90）"；切指标正确换数据源 |
| M6-T5 | 边界 | 无体征记录的患者 | 打开趋势图 | 显示"暂无数据"空态；窗口从最大化拖到半屏 → 图表跟随缩放不溢出、不重叠 |
| M6-T6 | 正向 | 提醒规则 | 把 cron 临时改成每分钟 → 等 2 分钟 | 工作台出现**红点** → 打开提醒列表看到记录 → 红点消失 |
| M6-T7 | 反向 | 上一步 | 重启应用两次；再把规则设为停用 | 同一时点**不出现重复条目**；停用后不再新增，历史记录仍在 |
| M6-T8 | 正向 | `P20260002` | 创建"降压管理方案"并勾选"每天 09:00 用药提醒" | 保存成功；M6-04 的提醒规则列表出现对应规则 |
| M6-T9 | 反向 | — | 为 `P20260003` 创建方案；起止日期填反 | 404；422 且指明字段 |
| M6-T10 | 正向 | `P20260001` | 做一次月度评估 → 患者详情"历史评估" | 出现该条，含结论、建议、评估人、时间 |
| M6-T11 | 反向 | `dr_li` 创建的评估 | `dr_wang` 尝试修改 | 403 |

---

### 3.8 M7 · 医生社区 ｜ ⛔ 仍不做 ｜ —

本期仍整体不做。**答辩口径（与《API-索引》§7 一致）**：

> M7 在老师原始需求中标注为 Optional，且与核心诊疗流程相对独立，工作量约等于整个问诊模块。为保证核心模块完成质量，整体列为二期规划。二期设计要点已保留：病例分享须手动脱敏后发布、与患者数据库物理隔离、模块可整体关闭。

---

### 3.9 M8 · 操作日志与审计 ｜ 🔴 横切 ｜ 依赖：M0、M1 ｜ 8h

**目标**：写操作全自动留痕、敏感读手动埋点、`audit_logs` 物理上不可篡改。

**交付内容**

| 编号 | 任务 | 交付物 | 旧 ID |
|------|------|--------|-------|
| M8-01 | 审计中间件（写操作自动 + **敏感读手动埋点**） | 中间件 + 埋点辅助函数 | T11 |
| M8-02 | 审计查询页（用户 / 时间 / 操作类型）+ CSV 导出 + 详情展开 | `AuditLogView` | T12 |
| M8-03 | 审计表结构补全（`user_id/ip/method/path/object_type/object_id/result/detail`）+ 索引 | 迁移 | T11 拆分 |

**完成标准**

- [ ] 所有 POST/PUT/PATCH/DELETE **自动记录**；每条带**语义化 `action`**（如 `patient.view`），而非只有 URL。
- [ ] **敏感读必须手动埋点**：患者详情查看、病历详情查看、审计日志导出——中间件看不到"这次 GET 读的是谁的数据"。
- [ ] 日志写入失败**吞掉并告警，不阻断主业务**。
- [ ] `audit_logs` **append-only**：无更新/删除接口，DB 账号亦无权限。
- [ ] CSV 导出：**与当前筛选一致**、文件名含时间范围、**UTF-8 BOM**。
- [ ] 索引齐全（`user_id` / `created_at` / `action`），单表 ≥ 1000 条时查询 < 2s。
- [ ] **仅 `admin` 可访问**，`senior` 也返回 403；菜单隐藏与接口 403 双保险。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M8-T1 | 正向 | `dr_wang` | 查看 `P20260001` 详情 | 新增一条 `patient.view`，`object_id=P20260001`，ip/result 正确 |
| M8-T2 | 正向 | — | 新建患者 | 新增 `patient.create`，`object_id` 为新患者 ID |
| M8-T3 | 正向 | 筛选"王医生 + 今天 + `patient.view`" | 导出 CSV | Excel 无乱码，行数 == 列表条数，时间范围与筛选一致 |
| M8-T4 | 安全 | 业务账号 | `DELETE` / `UPDATE audit_logs` | 两者均被拒绝 |
| M8-T5 | 反向 | 让日志库不可用 | 创建患者 | **患者仍创建成功**，不 500 |
| M8-T6 | 安全 | `dr_wang` 登录 | 地址栏直输 `/audit`；开 Network 面板 | 403 且**响应中不含任何日志数据**（返回了数据只是没显示 → 安全缺陷，不通过） |
| M8-T7 | 性能 | 单表 1000+ 条 | 三条件组合查询 | < 2s |
| M8-T8 | 正向 | — | 展开列表行 | 能看到 `detail` 原文（含关键字段值） |

---

### 3.10 M9 · 改造与横切收口 ｜ 🔴 必做 ｜ 贯穿 ｜ 16h

**目标**：把 Day 1–5 与契约不一致的既有实现一次性改到位，并统一前端质量底线。**这一组不做完，后面每接一个模块都要返工一次。**

**交付内容**

| 编号 | 任务 | 现状 → 目标 | 影响面（来自 §1.2） |
|------|------|------------|-------------------|
| M9-01 | 统一响应与异常封装 | `{"data":…}` / `{"error":{…}}` → `{code,message,data}` | `main.py` 异常处理器 + `schemas.py` + 前端 `client.js` + README + AGENTS.md |
| M9-02 | 分页统一 | `offset`/`limit` → `page`/`size`，返回 `{items,total,page,size}` | `main.py` + 前端 |
| M9-03 | 角色与科室基线对齐 | `administrator/doctor/department_manager` → `admin/senior/junior`；科室改 `信息科/心内科/神经内科` | `seed.py` + 迁移 + 前端权限判断 + 演示数据 |
| M9-04 | 健康检查对齐 | 去掉 503 语义门面冲突；补 `/health` 别名（与 `/api/health` 逐字节相同） | `main.py` |
| M9-05 | 前端三态与假数据清零 | 每个页面空/加载/错误态齐全；`demo-data.js` 只能通过 mock 开关使用 | 全部 View + `api/client.js` |
| M9-06 | 鉴权补齐既有接口 | `DELETE /api/patients/{id}` 等既有接口挂上 M1 的鉴权与数据范围 | `main.py` |

**完成标准**

- [ ] M9-01 ~ M9-04 在**同一个 PR 内完成**（破坏性改动，不允许留中间态）。
- [ ] `npm run build` 通过；既有前端页面挂掉的部分同步跟上。
- [ ] `demo-data.js` 的 import 全部改为按开关动态取数，任何 View 不得直接 import。
- [ ] 既有的 `/api/health/live` `/ready` 可保留（CI 依赖），但**不作为签收对象**，且不得与 `/health` 混淆。

**测试场景**

| 编号 | 类型 | 前置 | 操作 | 预期 |
|------|------|------|------|------|
| M9-T1 | 正向 | — | 请求不存在的路径；POST 缺必填字段 | 404 / 422，均返回 `{code,message,data}` 同构结构，**不是 HTML 错误页** |
| M9-T2 | 正向 | — | `GET /health` 与 `GET /api/health` | 响应逐字节相同 |
| M9-T3 | 正向 | 全新种子库 | 查 3 个角色 | `title` 为 `admin/senior/junior`；科室名为 `信息科/心内科/神经内科` |
| M9-T4 | 回归 | — | 跑 `uv run pytest -q` + `npm run build` | 全绿 |
| M9-T5 | 巡检 | — | 逐页打开 7 个模块页面 | 无控制台 `console.error`；无横向滚动；三态齐全 |
| M9-T6 | 安全 | 未登录 | 调所有既有接口 | 除白名单外全部 401 |

---

## 4. 模拟实现模块（S1–S6）

### 4.1 模拟实现的统一约定

| 约定 | 要求 |
|------|------|
| **provider 抽象** | 每个模拟模块必须有一个可替换的接口（`SmsProvider` / `FaceProvider` / `RecorderProvider` / `DeviceGateway`）。模拟实现是**其中一个实现**，不是把假逻辑写死在业务代码里 |
| **显式标注** | 所有模拟接口的响应体带 `"mock": true`；所有模拟界面有可见的"模拟"角标或横幅 |
| **红线** | **不得把模拟说成真实**。答辩 PPT、演示脚本、页面文案三处口径必须一致（见 §6.4） |
| **可替换性** | 接入真实服务的改动应局限于 provider 实现 + 配置项，不改业务代码 |
| **文档同步** | 动到 `openapi.yaml` 的（如 `vital.source` 增加 `device`），必须同步改 `API-索引.md` |

---

### 4.2 S1 · 触达通道模拟（短信验证码 + 短信/微信提醒） ｜ 8h

**模拟对象**：短信网关、微信模板消息。
**边界**：不出网、不接运营商、不接微信开放平台。

**交付内容**

| 编号 | 任务 |
|------|------|
| S1-01 | `NotifyProvider` 抽象 + `notify_outbox(id, channel[sms/wechat/email], target, template, payload_json, code, status, created_at)` |
| S1-02 | `MockSmsProvider` / `MockWechatProvider`：写入 outbox，不发真实消息 |
| S1-03 | M1-02 验证码服务支持 **channel 切换**（email / sms），复用同一套 TTL / 一次性 / 限流逻辑 |
| S1-04 | M6-04 生成提醒时同步写 outbox（channel = sms / wechat），形成"已推送"记录 |
| S1-05 | 演示用"模拟手机"浮层：登录页 / 提醒页可展开，显示最近 5 条 outbox 消息（**仅当前 ticket 或 admin 可见**） |

**完成标准**

- [ ] ⚠️ **必须书面记录的契约例外**：验证码"绝不出现在任何响应体与日志中"（T06 §5）与模拟出口表天然冲突。**处置**：`notify_outbox` 的验证码列仅在 `APP_ENV=dev` 下写入，生产 / 演示正式环境该列为空；`GET /api/notify-outbox` **仅 admin**，登录页浮层只返回**当前 ticket 关联的那一条**，不回表。
- [ ] 换渠道**不绕过限速**：短信与邮箱共用同一计数器。
- [ ] 真实邮件通道下 outbox **不落验证码明文**。
- [ ] 微信触达只落库，不做真实模板消息审核/回执。

**测试场景**

| 编号 | 类型 | 操作 | 预期 |
|------|------|------|------|
| S1-T1 | 正向 | 登录页切"短信验证" → 发送 → 提交收到的码 | 浮层出现 1 条 sms 记录含 6 位码；提交成功签发 JWT |
| S1-T2 | 反向 | 60 秒内重复发送（先邮箱后短信） | 429 且带剩余秒数；**换渠道不能绕开** |
| S1-T3 | 边界 | 同一码提交两次 | 第二次失败（一次性） |
| S1-T4 | 边界 | TTL 300s 后提交 | "验证码已过期" |
| S1-T5 | 安全 | 非 admin 调 `GET /api/notify-outbox` | 403 |
| S1-T6 | 边界 | SMTP 与 mock 同时不可用 | 返回明确错误、不吞异常；前端有超时，不无限等待 |
| S1-T7 | 正向 | M6-04 触发一次提醒 | outbox 出现对应 sms/wechat 记录，`status=sent(mock)` |

---

### 4.3 S2 · 人脸识别模拟 ｜ 6h

**模拟对象**：活体检测 + 人脸比对服务。
**边界**：不做真实生物特征比对；**不存储任何原始人脸图片**（只存 SHA-256 指纹）；演示只用合成图片。

**交付内容**

| 编号 | 任务 |
|------|------|
| S2-01 | `FaceProvider` 抽象 + `user_face(user_id, face_token, enrolled_at, status)`；`face_token` = 图片指纹哈希 |
| S2-02 | `POST /api/auth/face/enroll`：上传图片 → 存指纹（**不存原图**）→ 返回 `face_token` |
| S2-03 | `POST /api/auth/face/verify`：`ticket` + 图片 → 指纹一致返回 `{match:true, similarity:0.97, mock:true}`；否则 `{match:false, similarity:0.31}` |
| S2-04 | 前端人脸模态：`getUserMedia` 预览 + 扫描动画 + 成功/失败态；**摄像头不可用时降级为"选择图片"** |
| S2-05 | 登录 2FA 渠道配置：`email` / `sms` / `face` 三选一；人脸失败**计入与密码同一条限速计数器** |

**完成标准**

- [ ] 库中**查不到任何原始图片字节**；不存在 `uploads/faces/` 目录。
- [ ] 服务端**不信任客户端传入的任何判定字段**（响应无 `similarity`/`decision` 入参；传了也被忽略）。
- [ ] 所有响应带 `"mock": true`；UI 有"模拟识别"角标。
- [ ] 连续 5 次人脸失败 → 账号锁定，与密码失败共用计数器。

**测试场景**

| 编号 | 类型 | 操作 | 预期 |
|------|------|------|------|
| S2-T1 | 正向 | 已录入 → 提交同一图片 | `match:true` → 签发 JWT |
| S2-T2 | 反向 | 未录入即验证 | 提示"请先录入人脸"，**不签发** |
| S2-T3 | 反欺诈 | 直接调 API 并在请求体里塞 `match:true` / `similarity:1.0` | 服务端忽略，仍按指纹判定 |
| S2-T4 | 反向 | 连续 5 次失败 | 账号锁定 / 429；换人脸渠道**不能重置**密码失败的计数 |
| S2-T5 | 隐私 | 查库 + 查上传目录 | 只有 hash；无原图文件 |
| S2-T6 | 边界 | 浏览器禁用摄像头 | 前端给出"选择图片"降级路径，**不白屏** |
| S2-T7 | 标注 | 检查接口与页面 | 响应含 `mock:true`；页面有可见角标 |

---

### 4.4 S3 · 会话录像模拟 ｜ 8h

**模拟对象**：通话录音录像与云端点播。
**边界**：**不产生任何音视频文件**；只重建通话**事件时间线**。

**交付内容**

| 编号 | 任务 |
|------|------|
| S3-01 | `call_recording(call_id, timeline_json, duration, ended_abnormally, created_at)`；时间线由 WS 信令事件 + 每 5s 心跳重建 |
| S3-02 | `GET /api/consultations/{id}/calls/{call_id}/recording` 返回时间线 |
| S3-03 | 回放页：进度条 + 事件刻度 + 与图文消息对齐的文字轨；**顶部固定"模拟回放 · 无音视频"横幅** |
| S3-04 | `GET .../recording/print` → Jinja2 通话记录单（可打印） |
| S3-05 | 异常中断兜底：未收到 end 时以最后心跳为准，标 `ended_abnormally: true` |

**完成标准**

- [ ] 回放页与打印页**都含"模拟"标注**；导航栏不许写"录像回放"四个字单独出现。
- [ ] 时长与 `call_log.duration` **一致**（同一个计算来源）。
- [ ] 无通话 / 进行中的通话 → 404；非参与人 → 403。

**测试场景**

| 编号 | 类型 | 操作 | 预期 |
|------|------|------|------|
| S3-T1 | 正向 | 通话 60 秒 | 生成 recording，时长 60±2s；时间线含 呼叫/接通/心跳×12/挂断 |
| S3-T2 | 反向 | 进行中即访问；非参与人访问 | 404；403 |
| S3-T3 | 边界 | 通话中一方断网 | `ended_abnormally=true`，时长取最后心跳，**不产生无限时长** |
| S3-T4 | 一致性 | 比对回放页时长与 `call_log.duration` | 相等 |
| S3-T5 | 红线 | 检查页面 / 打印页 / 演示脚本 | 三处均为"模拟"口径 |

---

### 4.5 S4 · IoT 体征采集模拟 ｜ 10h

**模拟对象**：可穿戴设备 / 床旁监护仪。
**边界**：无真机；模拟器走**与真实设备相同**的上报接口，不写后门。

**交付内容**

| 编号 | 任务 |
|------|------|
| S4-01 | `device(id, device_code, patient_id, model, status, last_seen_at, battery, device_token_hash)`；`vital_sign.source` 扩为 `manual/mock/device`（**契约变更，须同步 `openapi.yaml` 与 `API-索引.md`**） |
| S4-02 | `POST /api/devices/{device_code}/vitals`：`X-Device-Token` 鉴权（**非 JWT**），支持单条/批量，写入时做阈值判定 |
| S4-03 | `scripts/device_simulator.py`：按 5 分钟节拍推送真实感曲线（基线 + 正态抖动 + 计划内异常点），`--speed` 加速演示 |
| S4-04 | 设备管理页：在线/离线、最后上报、电量、绑定患者；上线/下线/换绑 |
| S4-05 | 心跳接口 + APScheduler：超过 3 个心跳周期未上报 → 自动置 `offline` |

**完成标准**

- [ ] 上报接口鉴权走 **device_token**，与用户 JWT 是两套；设备 token 泄露影响面局限在单设备。
- [ ] 设备通道**不绕过 M1-05 数据范围**：junior 查绑到外科室患者的设备数据仍 404。
- [ ] 同 `(device_code, recorded_at)` 重复上报**不产生重复点**。
- [ ] 契约变更（`source` 增加 `device`）已在 `openapi.yaml`、`API-索引.md`、前端类型三处同步。

**测试场景**

| 编号 | 类型 | 操作 | 预期 |
|------|------|------|------|
| S4-T1 | 正向 | 启动模拟器 | 设备页变 online；5 分钟内趋势图出现新点且 `source=device` |
| S4-T2 | 反向 | 错误 device_token；已解绑设备上报 | 401；404 |
| S4-T3 | 正向 | 模拟器推 180/110 | 该点 `is_abnormal=true`，趋势图标红 |
| S4-T4 | 安全 | 设备绑神经内科患者，`dr_wang` 查该患者体征 | **404** |
| S4-T5 | 边界 | 同 `(device_code, recorded_at)` 上报两次 | 只产生 1 个点（更新或拒绝，二选一并写进文档） |
| S4-T6 | 边界 | 停掉模拟器 | 3 个心跳周期后设备变 offline，显示最后上报时间 |
| S4-T7 | 红队 | 用普通用户 JWT 调设备上报接口 | 401（两套鉴权不互通） |

---

### 4.6 S5 · 企业级监控面板模拟 ｜ 12h

**模拟对象**：Prometheus + Grafana + 告警。
**边界**：不部署 Prometheus / Grafana 容器；指标自采、自绘（复用已引入的 ECharts）、自警。

**交付内容**

| 编号 | 任务 |
|------|------|
| S5-01 | 指标采集中间件：请求计数、耗时直方（算 P95）、错误计数、按 path 分桶；环形缓冲 + 定期落 `metric_sample` |
| S5-02 | `GET /metrics`：**Prometheus 文本格式**（`text/plain; version=0.0.4`）——**全项目唯一不遵 `{code,message,data}` 封装的接口**，必须在契约中显式写明 |
| S5-03 | 管理端"系统监控"页：QPS / P95 / 错误率折线、服务卡片（db / redis / APScheduler / WS 在线数）、慢接口 TopN、最近错误 |
| S5-04 | 简易告警：错误率 > 5%（5 分钟窗口）或 P95 > 1s → 页面横幅 + 站内通知 |
| S5-05 | 采集开关与开销上限：采样率可配，关闭时中间件零开销 |

**完成标准**

- [ ] `/metrics` 的输出格式可被 `promtool check metrics` 解析（无 promtool 时用正则自检并写成测试）。
- [ ] `/metrics` 的暴露范围**必须显式决策**（见 §7 待决策 1），默认不公开暴露。
- [ ] `/api/monitor/*` 仅 `admin`。
- [ ] 采集开启时 P95 增幅 < 10%。
- [ ] 与 M0-03 口径一致：**依赖挂掉不改状态码**，只在服务卡片显示 degraded。

**测试场景**

| 编号 | 类型 | 操作 | 预期 |
|------|------|------|------|
| S5-T1 | 正向 | 压 100 次请求 | 监控页 QPS / P95 与手工计算一致（±10%） |
| S5-T2 | 边界 | 停 Redis | 服务卡片 `redis=down`，页面不崩 |
| S5-T3 | 安全 | 非 admin 调 `/api/monitor/*` | 403 |
| S5-T4 | 正向 | 人为制造 5xx | 5 分钟内出现告警横幅 + 站内通知 |
| S5-T5 | 格式 | 拉 `/metrics` | 文本格式合法；无 `code/message/data` 包裹 |
| S5-T6 | 性能 | 对比开关采集 | P95 增幅 < 10% |

---

### 4.7 S6 · 动态权限配置模拟 ｜ 10h

**模拟对象**：企业级 RBAC 管理后台。
**边界**：只做"角色 × 功能权限"矩阵；**数据权限仍由 `department` + `temp_grant` 决定**，不做数据级可视化配置。

**交付内容**

| 编号 | 任务 |
|------|------|
| S6-01 | `permission(code, module, name)` + `role_permission(role_id, permission_id)` 建表；启动时把内置默认矩阵幂等写库 |
| S6-02 | 权限校验改为查库（带缓存），**保留内置默认矩阵作为兜底**（库不可用时回落到默认，**不放开**） |
| S6-03 | admin 页面：角色 × 权限勾选矩阵；改动**即时生效**（缓存失效） |
| S6-04 | 防自锁：不允许移除 `admin` 的 `permission.manage`；不允许删除最后一个拥有该权限的角色 |
| S6-05 | 变更留痕：`permission.grant` / `permission.revoke`（含前后值）；一键回滚到默认矩阵 |

**完成标准**

- [ ] 兜底方向必须是**保守**：权限数据源故障 → 回落到内置默认，**绝不能变成全部放开**。
- [ ] 矩阵改动**无需重启**即生效；前端菜单可见性与后端 403 一致。
- [ ] 自锁保护有明确文案（409）。

**测试场景**

| 编号 | 类型 | 操作 | 预期 |
|------|------|------|------|
| S6-T1 | 正向 | 取消 `junior` 的 `patient.read` | 不重启，`dr_wang` **立刻 403**；恢复后立刻可读 |
| S6-T2 | 反向 | 尝试取消 admin 的权限管理权 | 409 + 明确文案 |
| S6-T3 | 兜底 | 清空 `permission` 表 | 仍按内置默认矩阵鉴权；**不得全部放开** |
| S6-T4 | 留痕 | 查审计 | 有 grant / revoke 前后值；回滚动作也有记录 |
| S6-T5 | 一致性 | 改矩阵后看前端 | 菜单可见性与接口 403/200 一致（不能只隐藏菜单） |

---

### 4.8 S7 · 患者端模拟 ｜ ⚪ 可选 ｜ 6h

**模拟对象**：患者 App / 小程序。
**边界**：只读患者视角，**不是完整 App**；用一次性链接访问，不接真实患者登录体系。

**交付内容**：移动端宽度的只读页面（本人体征趋势 / 健康方案 / 提醒记录）；`POST /api/patient-links` 生成一次性链接（TTL 24h，可撤销）；访问 → 越权与他人数据一律 404。

**测试场景**：链接过期 → 401/404；改 URL 中的患者号查他人 → 404；只读页面**无任何写入口**。

---
