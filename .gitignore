.env
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc
*.db
*.db.bak*
node_modules/
dist/
backups/

# A Redis unpacked by backend/start.* when none was installed. Binaries must
# never be committed: the startup script re-downloads them on demand.
backend/runtime/

# T31's shared material store. Demo uploads must never reach the repository.
uploads/
