# Copy to .env.server; never commit real credentials.
JWT_SECRET=
PATIENT_DATA_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_STARTTLS=true
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=
# Local reverse proxy terminates HTTPS and forwards to 127.0.0.1:8080.
HTTP_BIND=127.0.0.1
HTTP_PORT=8080
