# 医生工作平台 · 需求完成现状

> **核对时点**：2026-09-21，`main` 分支工作区。本次是**逐条对代码整体复核**，不是增量更新——下面每一行的数字和结论都在本次核对中重新取过。
> **需求依据**：`docs/01-任务安排.md`（M0–M9 / S1–S7 / D01–D07 及各自「完成标准」）。
> **核对方式**：后端路由实测（`app.openapi()`）、契约与实现的集合差、迁移链与 `scripts/build_db.py` 实跑、前端路由与取数实读、CI 配置、两套测试**实际执行**。
>
> ⚠️ **本文是派生视图，不参与 `docs/README.md` 的权威顺序。** 状态列的权威在 `docs/01-任务安排.md` §2，「建成了什么」的权威在代码、其次是 `docs/03-实现现状.md`。README 的维护约定写明「不要再新增并列的『权威』文档」——本文按核对需要存在，**结论应回落到 01 §2 与 03，而不是让本文取而代之**。本文与代码冲突时，以代码为准。

---

## 1. 一句话结论

**M3 / M4 / M5 三个主模块主体已交付；M0 / M1 / M2 / M6 / M8 / M9 部分完成；S1 是标注齐全的模拟；S2 代码最完整却因缺接口标注而触红线；S3–S6 仍零代码。**

三个必须立刻知道的事实：

1. **`main` 是绿的**：后端全量回归 **252 passed**（370.61s，2026-09-21 复测；233 passed 是加入全流程种子层之前的数），前端 **52 passed**，隔离空库建库校验通过。没有失败用例，没有 xfail 掩盖。
2. **S2 人脸模拟未过红线**：响应体里没有 `"mock": true`。`POST /api/auth/face/login` 返回的是通用 `TokenResponse` → `TokenData`，其 `required` 只有 `[access_token, token_type, expires_in, user]`（`docs/api/openapi.yaml:3509-3519`），实现同样只回这四个键（`face_login.py:63-65`）。按 01 §4.1「红线细则三条，缺一即判未通过」，**S2 判定为未通过**——尽管它的代码是六个模拟模块里最完整的。
3. **有一处中文 UI 字符串是「故意」的**，与 CLAUDE.md「前端文案一律英文」的硬规则正面冲突，需要人工裁决而不是当成笔误改掉（§5.1）。

---

## 2. 本次核对覆盖到什么

| 维度 | 实测值 | 取数方式 |
|---|---|---|
| 契约声明 | `openapi.yaml` **93 条路径 / 113 个操作** | 解析 YAML |
| 后端实测路由 | **93 条路径 / 127 个操作** | `create_app(Settings(...)).openapi()`，2026-09-21 |
| 契约 ↔ 实现集合差 | 契约多 9 条、实现多 9 条；**真缺口的只有 1 条**（见 §4.1） | 两侧 `paths` 取差集 |
| WebSocket | `/ws/chat/{room_id}` 已实现（`chat.py` 的 `socket_router`，挂在 `main.py:337`）。**它不出现在 `app.openapi()` 里**——FastAPI 不为 WebSocket 生成 OpenAPI 条目，所以集合差会把契约侧的 `/ws/chat/{room_id}` 报成「缺失」，那是工具假象 | 枚举 `socket_router.routes` |
| 默认鉴权 | `security.py` 的 `ProtectedRoute` 对非白名单路径一律注入鉴权；匿名白名单 **13 条**（`security.py:34-50`） | 读常量 |
| 迁移链 | **18 个 revision 文件**，唯一 head = `d5e8a3b71c04` | 列目录 + 建库实测 |
| 建库契约 | **37 张表**（36 个模型表 + `alembic_version`）+ 2 个 append-only 触发器；`alembic check` 无模型差异 | **实跑** `scripts/build_db.py --db backend/runtime/check.db` |
| 前端路由 | **20 条**（含重定向与 `/:pathMatch(.*)*` 兜底）；**角色门禁三条**：`/review`、`/audit`、`/users`（`router/index.js:119,125,133`） | 读 `router/index.js` |
| 前端 mock 面 | 见 §4.4——旧记录「唯一 mock 源是 `mock-auth.js`」不准确 | 全量 grep 导入关系 |
| 后端测试 | **22 个测试文件 / 252 passed / 0 failed**（370.61s） | **实跑** `uv run pytest -q` |
| 前端测试 | **11 个测试文件 / 52 passed / 0 failed**（419ms） | **实跑**（Windows 下需显式列文件，见 §6） |
| CI | `.github/workflows/ci.yml`：pytest + ruff + 前端构建 + Docker 全栈冒烟 + 服务验证 + 备份；**无契约校验步骤** | 读 workflow |

---

## 3. 模块状态总览

> 状态列口径与 `01` §2 一致：无代码不写完成；有代码但场景未逐条走查，最多「部分完成」；模拟实现一律标注。

| 模块 | `01` §2 状态 | **本次核对结论** | 判断依据（代码） | 仍缺什么 |
|---|---|---|---|---|
| **M0** 基础设施与契约 | 部分完成 | **部分完成** | `backend/start.*`、迁移链、`/health` 别名齐备；`build_db.py` 36 表校验实跑通过 | **M0-05 契约校验仍未做**——CI 无该步骤，仓库无比对脚本（`generate_auth_schemas.py` 只生成类型，不比集合） |
| **M1** 认证与数据范围 | 部分完成 | **部分完成** | 认证与字典共 **15 条路径 / 18 个操作**（`/api/auth*` 10 + `/api/temp-grants` 2/3 + `/api/users` 2/4 + `/api/roles` 1）；`security.py` 默认保护；匿名白名单 13 条 | **M1-08 人脸失败计数确认未做**：密码路径有 5 次 / 300s 限速（`auth.py:174-190`），**人脸路径完全没有计数器**（`face_login.py` 全文无限速逻辑），因此谈不上「共用同一计数器」。M1-07 已落地 |
| **M2** 患者与过敏 | 部分完成 | **部分完成（M2-04/05/06 已补齐，剩验收）** | 患者/过敏/分组/**病史**共 **10 条路径 / 20 个操作**；AES-GCM 加密 + 盲索引；**症状标签已结构化**（见 §4.3）；分组六条路由齐备且**有界面** | **代码已齐**：M2-04 详情页（警示条 / 分组 / 症状标签 / 三 Tab / 两个编辑器）、M2-05 `Manage groups`、M2-06 病史竖切（`patient_history` 表 + 迁移 `d5e8a3b71c04` + 四条路径 + 时间线）。**缺的是人工验收**：`02 §2.3` 的场景未逐条走查；M2-T4 措辞与代码不符（§5.3） |
| **M3** 在线问诊 | 部分完成 | **部分完成** | `/api/consultations*` **8 条路径**（连同受保护上传共 **10 条路径 / 13 个操作**）+ WebSocket `/ws/chat/{room_id}` + WebRTC 信令与通话元数据；`test_m3.py`；`rooms.py`（共享房间解析）+ 共享 `RoomPanel.vue` | 真实音视频硬件、局域网 HTTPS 仍需**人工验收**；与 M5 的房间复用已落地（`m<id>` 键、`test_m5_room.py`，见 M5 行）——是「未验收」不是「没写」 |
| **M4** 病历与检索（EMR） | 已完成 | **已完成** | `emr.py` 15 条路径/20 操作；迁移 `7ad4cb527bd2`；`test_emr.py`；`MedicalRecordView` / `ReviewQueueView` 接真实接口 | 无。六格矩阵与浏览器流程为人工实跑记录（见 03），本次未复跑 |
| **M5** 会诊 | 已完成 | **已完成** | `meetings.py` **11 条路径 / 14 个操作**（含 `/api/materials/{id}/download`）；房间复用：`rooms.py` + `chat.py` 的 `meeting_router`（`/api/meetings/{id}/messages|calls`）+ `test_meetings.py` + `test_m5_room.py`；`RemoteConsultationView` 接真实接口并挂载共享 `RoomPanel.vue` | 02 §2.6 的会诊场景未逐条走查；房间复用只到接口与前端单测级——浏览器端到端（Playwright）本机未安装，未跑 |
| **M6** 体征与设备 | 部分完成 | **部分完成** | `health.py`；`test_health.py`（日期腐坏已修）；`PatientHealth.vue` 接真实接口 | 02 §2.7 的 11 条场景未逐条走查 |
| **M7** 医生社区 | 不建 | **不建** | — | 仅保留答辩口径 |
| **M8** 审计 | 部分完成 | **部分完成（仅剩单条详情）** | `GET /api/audit-logs`（`audit.read`）、`/export`（`audit.export`）、`/actions`（动作字典，`audit.read`）已实现；`AuditLogView` 接真实接口 + 服务端筛选 + CSV + 服务端动作下拉 | 仅 `/api/audit-logs/{id}` 未实现（01 的 T12 口径本就不做） |
| **M9** 收口 | 部分完成 | **部分完成（基本收口）** | M9-01~04 完成；`ProtectedRoute` 覆盖既有接口 | 残留死代码 `api/demo-data.js` 仍在仓库里；M9-05 的三态只做了「无假数据」这一半 |
| **S1** 短信验证模拟 | 部分完成（模拟） | **部分完成（模拟），标注齐全** | 后端 `sms.py` + `notify_outbox` 表 + `GET /api/notify-outbox`（admin 专属，`sms.py:142`）；响应体带 `mock: true`（`sms.py:98,125,138,166`，契约 `openapi.yaml:3342-3365`）；前端 `SmsCodeInput.vue` + 选项文案 `SMS (simulated)`（`LoginView.vue:33`） | 不发送真实短信；无短信注册、微信/M6 提醒（S1-04）、全局最近 5 条浮层（S1-05） |
| **S2** 人脸识别模拟 | 已完成（模拟） | **未通过（红线）** | `POST /api/auth/face/login` 存在；`match_face()` 恒 `True`（`face_login.py:18-21`） | ❌ 响应体**无 `"mock": true`**；❌ `/enroll`、`/verify`、`/image/{ref}` 后端全无，由**前端 mock** 顶替 |
| **S3** 会话录像模拟 | 未开始 | **未开始** | 零代码：无表、无路由、前端无 `MediaRecorder`（全仓 grep 无命中） | 全部 |
| **S4** IoT 体征采集模拟 | 未开始 | **未开始** | 零代码；契约 `vital_sign.source` 枚举为 `[manual, mock]`（`openapi.yaml:4991`），**没有 `device`** | 全部；含契约变更（`source` 增 `device`） |
| **S5** 监控面板模拟 | 未开始 | **未开始** | 零代码；无 `/metrics` 路由（全仓 grep 无命中）、无 `metric_sample` 表 | 全部 |
| **S6** 动态权限配置模拟 | 未开始 | **未开始** | `models.py` / `emr_models.py` / `work_models.py` 三个模型文件共 35 张表，**无 `permission` / `role_permission`** | 全部 |
| **S7** 患者端模拟 | 不建 | **不建** | — | — |
| **D01–D07** 文档与交付 | 部分完成 | **无法逐项核对** | 01 §7.1 只把 `M01–M07` 映射为 `D01–D07`，§7.2 仅一句「D01–D07 的细化条目」 | **条目定义在当前文档集里不存在**——「交付物齐全」这条判据因此无法判定（§5.2） |

---

## 4. 需要展开说明的四条

### 4.1 M0-05：全项目唯一仍然「零代码」的横切项

`docs/01` §2 把它列为 M0 的唯一缺口，**至今未动**：`.github/workflows/ci.yml` 的步骤只有 pytest / ruff / 前端构建 / Docker 全栈冒烟 / 服务验证 / 备份，**没有任何契约校验**；仓库内也不存在做路由集合比对的脚本。

本次把两侧按路径名做了一次集合差（`app.openapi()["paths"]` vs `openapi.yaml` 的 `paths`），差异分三类，**共 9 条**（M2-06 落地后从 11 条降到 9 条）：

**一、只差路径参数名，路由本身在（7 条）**——脚本要么规范化参数名，要么把这一类比掉：

- `/api/allergies/{id}`（实现写作 `/api/allergies/{allergy_id}`）
- `/api/assessments/{id}`（`{assessment_id}`）
- `/api/health-plans/{id}`（`{plan_id}`）
- `/api/reminder-rules/{id}`（`{rule_id}`）
- `/api/reminders/{id}/done`（`{reminder_id}`）
- `/api/patient-groups/{id}`（`{group_id}`）
- `/api/patient-groups/{id}/members`（`{group_id}`）

> M2-06 的四条新路径**用的是契约的 `{id}`**，不是 `{history_id}`：否则补齐一处就变成再添一条豁免，把 7 条撑到 8 条。上表最后两条属 M2-05，本次**没有顺手改**——参数名统一是 M0-05 的事，一个 M2 的 PR 不该替另外四个模块的路径一起改名。

**二、真的没有实现（1 条）**——这才是「契约有、代码无」的实底：

- `/api/audit-logs/{id}`——**故意不做**，见 §3 的 M8 行；

> 此前这里还有 `/api/patients/{patient_no}/histories` 与 `/api/histories/{id}` 两条（M2-06 病史）。**本次已实现**，两条路径从差集里消失。

**三、工具假象（1 条）**：`/ws/chat/{room_id}` 在 M3 里是**实现了**的，但 FastAPI 不为 WebSocket 路由生成 OpenAPI 条目。写比对脚本时要么排除 `ws` 前缀，要么改为从 `app.routes` 取并集。

反方向「实现有、契约无」共 9 条：7 条参数名对偶，加 `/api/health/live`、`/api/health/ready`（`/health` 与 `/api/health` 这对别名**契约里是有的**）。

> **口径建议**：先把这 7 条参数名统一（改契约比改实现便宜，且不触碰已验证的路由），再把仅剩的 1 条真缺口列成显式豁免清单，脚本即可上线。否则脚本一上线就是红的。

### 4.2 M0-05 之外的横切缺口：无

除契约校验外，`01` §2 列出的 M0 完成标准（建库一次性成功、`/health` 逐字节别名、`{code,message,data}` 统一封装、分页统一、`audit_logs` 由 `c0311060809b` 触发器保证 append-only 且 SQLite/MySQL 两种方言都建）**本次均实测通过**。附录「文档与代码不符」的两处已在 §5 单列。

### 4.3 M2：症状标签**已经结构化**了——旧记录这条是错的

> 此前本文写作「`symptom_tags` 仍是 `patients` 表上的 `String(500)` 拼接串，靠 `LIKE` 匹配」。**这条与代码不符，本次更正。**

`PatientTag` 模型（`models.py:114-130`）把标签拆成了独立的 `patient_symptom_tags` 表：`patient_id` + `tag` 两列，`tag` 带索引，并对 `(patient_id, tag)` 加了唯一约束。模型自己的 docstring 就写明它「replaced a `String(500)` column holding `",tag,tag,"`」，理由是任务清单与契约都要求标签「结构化以支持搜索」，而对拼接串做子串匹配不是。

搜索侧也确实按结构化走：`main.py:597-600` 用的是 `PatientTag.tag.in_(symptom_tags)` 的**精确匹配**，参数可重复传（`main.py:506-511`）。因此「四条件搜索里症状一项是模糊串匹配」同样不再成立——现在是等值命中，`胸痛` 不会被 `胸` 匹配到。

**此前缺的 M2-04 / M2-06 已补齐。** 详情页现在有「Consultation records」「Medical history」「Health data」三个 Tab，身份头下方是过敏警示条（`data-severity`，设计系统里唯一合法的红色）、症状标签与分组；过敏与病史各有编辑器。病史四条契约路径全部实现，`patient_history` 表随迁移 `d5e8a3b71c04` 落地。M2-05 的分组六条路由此前只有读，现在列表页的 `Manage groups` 是它们的写入口。

**M2 目前仍缺的是验收，不是代码**：`02 §2.3` 的场景需人工走查；`docs/02` M2-T4 的措辞与代码不符（见 §5.3）。

### 4.4 前端 mock 面：比旧记录复杂，且有一半是死代码

此前本文概括为「`api/demo-data.js` 已无导入者；唯一 mock 源是 `api/mock-auth.js`，由 `USE_MOCK_AUTH` 开关控制」。本次逐文件核对后需要修正：

| 文件 | 实况 |
|---|---|
| `api/demo-data.js` | **确实是死代码**：全仓没有任何 `import`，5 处提及全部是注释（`App.vue:61`、`router/index.js:82`、`DashboardView.vue:14`、`RemoteConsultationView.vue:19`、`mock-audit.js:8`）。建议删除（§5） |
| `api/mock-auth.js` | 仍活着，但**只有 face 那一半**被用到。`client.js:4` 只导入 `face as mockFace`；`faceEnroll`(`:583`)、`faceVerify`(`:587`)、`faceImageUrl`(`:598`) 三处按 `USE_MOCK_AUTH` 分流 |
| ↑ 的 sms 那一半 | **死代码**：`sms.send` / `sms.verify` / `maskPhone` 在 `src/` 下**没有任何导入者**，只被 `tests/mock-auth.test.js` 使用 |
| ↑ 的 `face.verify` | **也是死代码**：`faceVerify` 在 `src/` 下只有 `client.js:587` 这一处定义，没有调用方。登录页的人脸渠道走 `face-login.js` → `authentication.faceLogin` → **后端** `POST /auth/face/login`（`client.js:573`） |
| `USE_MOCK_AUTH` | `client.js:9` **硬编码 `true`**，不读环境变量；且只约束上面三处 face 调用 |
| `api/mock-audit.js` | **活着**，由 `mock-auth.js:12` 导入并记录模拟操作；但它写入的是内存数组，**没有任何 UI 消费**——`AuditLogView.vue:5,115,196` 读的是真实 `audit` 接口 |
| 可见的「模拟」标注 | `MockBadge` 仅 2 个调用点：`LoginView.vue:321`、`FaceEnrollView.vue:77`。短信的说明文字在 `SmsCodeInput.vue:62,143-145`；`FaceEnrollView.vue:127-139` 另有「What is simulated」面板 |

**关键更正**：登录页的短信渠道**不是**前端 mock 在顶替，它走的是**后端 S1 接口**（`client.js:576-578` 的 `smsSend` / `smsPreview` / `smsVerify` 一律走网络）。前端那份 `mock-auth.js` 的 sms 实现只是历史遗留、无人调用。

---

## 5. 需要人工裁决 / 建议处理

### 5.1 中文 UI 字符串：`MockBadge` 的「模拟环境」角标与 CLAUDE.md 硬规则冲突

CLAUDE.md 的硬规则是「All user-facing frontend text is English. Do not add Chinese UI strings.」，但 `MockBadge.vue:17` 的默认 label 是 `'模拟环境'`，且**两个调用点都直接用 `<MockBadge />`、不传 label**，所以登录页与人脸录入页各渲染一次中文。

这不是笔误：`MockBadge.vue:11-14` 注明该字符串是规格逐字规定的，`docs/T41短信登录与T42人脸识别-任务详设.md` 也确有 7 处「模拟环境」（`:34,166,318,341,561,618,652`），并在 `:618` 指明 `MockBadge.vue` 由 T41/T42/T44/T45/T46 共用。

**裁决口径二选一**：① 认定该角标是英文规则的**书面例外**，在 CLAUDE.md 里写清豁免；② 改英文（如 `Simulated`）并同步改 T41 详设的对应处。**建议 ①**——红线要求的正是「界面上可见的模拟角标」，而规格已经把它逐字固定下来了，为一个词去改两处文档再加一次验收不划算。**无论选哪个，都应当在 CLAUDE.md 里留一行，否则下一个人会当成违规改掉。**

### 5.2 其余两条待决

| # | 事项 | 为什么需要人裁决 | 建议 |
|---|---|---|---|
| 1 | **给 `/api/auth/face/login` 的响应体补 `"mock": true`** | 这是 S2 过红线的**唯一**阻塞项。成本极小（改 `face_login.py` + `openapi.yaml` 的 `TokenData`，注意 `TokenData` 被密码登录共用，加字段需做成可选或另立 schema） | 做。补完 S2 即可从「未通过」转为「已完成（模拟）」 |
| 2 | **补 `D01–D07` 的条目定义** | 「交付物齐全」是对 Day 15 / MS7 的验收判据，而当前文档集里没有 D01–D07 每一项的定义，判据无法判定（§3 表中该行「无法逐项核对」即由此而来） | 补进 `01` §7.2 那张表 |
| 3 | **删除死代码 `api/demo-data.js`** | 已无导入者，但留着就是一份随时会被误读的假数据源；`router/index.js:82` 的注释还在指向它，会让人以为它仍在使用 | 连同 `router/index.js:82` 的过期注释一起删 |
| 4 | **定 M0-05 契约校验的参数名口径并落脚本** | 契约与实现继续无人拦截；§4.1 的 9 条差异会越积越多 | 按 §4.1 的口径建议先统一 7 条参数名 |

### 5.3 `docs/02` M2-T4 的「422」与代码不符——测试按代码写，措辞待订正

`docs/02-测试场景.md` §2.3 的 M2-T4 期望「客户端自带 `patient_no` 时返回 422」。**代码不是这样**：`PatientCreate` 的 `model_config` 是 `extra="ignore"`（`schemas.py:218`），未知字段被静默丢弃，请求返回 **200**，患者被创建，而 `patient_no` 一列由服务端生成（`P20260001` 这种），客户端送来的值根本没有落库的机会。

本次补了这个场景的自动化覆盖（`tests/test_api.py::test_a_client_supplied_patient_number_is_ignored_rather_than_written`），**断言的是代码的实际行为**，不是场景文档的措辞：不单方面改规范，也不写一条会红的测试。

**待裁决**：把 `docs/02` M2-T4 的期望从「422」改成「忽略并按服务端编号建号」，或者在契约与实现上补一条真正的 422（`extra="forbid"`）。前者成本更低，且「服务端编号不容客户端投票」这个语义本身是对的——`extra="ignore"` 是现有实现的选择，不是缺陷。**建议前者**，由 `docs/02` 的维护者改。

---

## 6. 怎么复核本文

```bash
# 1. 后端路由（§2）——注意：不要用 app.routes 数，那条路在当前 FastAPI 上数不全
#    （include_router 的结果是嵌套的 _IncludedRouter，不摊平进 app.routes）。
cd backend && uv run python -c "
from app.main import create_app; from app.config import Settings
app = create_app(Settings(redis_url=None, scheduler_enabled=False, _env_file=None))
spec = app.openapi(); print(len(spec['paths']), 'paths')"

# 2. 契约 ↔ 实现的集合差（§4.1）——把两边的 paths 取差集，再分类
#    （WebSocket 路由不出现在 app.openapi() 里，比对时要单独排除，否则会误报成缺失）
cd backend && uv run --with pyyaml python -c "
import yaml,pathlib
from app.main import create_app; from app.config import Settings
app = create_app(Settings(redis_url=None, scheduler_enabled=False, _env_file=None))
impl=set(app.openapi()['paths'])
contract=set(yaml.safe_load(pathlib.Path('../docs/api/openapi.yaml').read_text(encoding='utf-8'))['paths'])
print('contract-only', sorted(contract-impl)); print('impl-only', sorted(impl-contract))"

# 3. 全量回归（§2）
cd backend && uv run pytest -q

# 4. 建库契约（§2）——只对隔离库跑，不要覆盖正在手测的 backend/doctor.db
uv run --project backend python scripts/build_db.py --db backend/runtime/check.db

# 5. 前端测试（§2）——Windows 下 npm test 的 glob 不展开，需显式列文件
cd frontend && node --test (Get-ChildItem tests -Filter *.test.js).FullName
```

---

*本文 2026-09-21 逐条对代码整体复核后重写，取代此前基于首版增量更新的版本。**状态权威见 `docs/01-任务安排.md` §2，代码现状权威见 `docs/03-实现现状.md` 与代码本身。** 本文不参与 `docs/README.md` 的权威顺序，核对结论应回落到上述文档。*
