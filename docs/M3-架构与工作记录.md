# M3 架构与工作记录

日期：2026-09-18。负责人：李奕儒。范围由 `01-任务安排.md` §3.4 和 `02-测试场景.md` §2.4 定义。

## 本次整合

- 从原分支 `codex/b-w2-w3` 的 `56b5f9e` 起，在独立工作副本合并 `origin/main` 的 `a2a6de3`。保留新版认证、角色路由守卫、审计查询、SQLite 技术选型、演示基线和启动脚本。
- 保留旧分支已有图文问诊、医嘱对接、健康方案与提醒代码；后面三项属于其他模块的历史增量，不在本次扩展其业务范围，也不把它们作为 M3 完成依据。
- `.gitignore` 合并两侧规则。新增 merge revision `f18a03c94b62`，同时承接 `786395e6a0ba`（main 审计）和 `bd9e76dbea39`（旧问诊等业务表）。新增 `a9c327e80f16` 创建 call_log、会话时间索引。保持已发布旧 revision 不变。
- `scripts/build_db.py` 同步登记合并后的 17 张表（含 alembic_version）和两个 append-only 审计触发器；可为不存在的验收库目录创建父目录。
- 开发入口默认 Docker Redis + 本机 Vite + Uvicorn reload，首次生成私密本地配置；不提交数据库、密钥、Token、上传图片或依赖目录。

## 文件与职责

| 层 | 文件 | 职责 |
|---|---|---|
| 应用入口 | `backend/app/main.py` | 注册路由、统一异常与审计、数据库/Redis、定时任务、视频超时清理与退出清理 |
| 鉴权 | `auth.py`、`security.py`、`patients.py` | JWT、撤销/禁用检查、声明式权限、科室/临时授权范围 |
| 问诊 | `chat.py` | waiting → active → ended、参与者检查、先持久化再广播、幂等消息、上传与受保护图片、WS 在线租约 |
| 记录 | `consultation_records.py` | 患者号/姓名 + 日期 + 消息关键词 AND 过滤、分页、BOM CSV、通话记录读取/手工上报 |
| 视频信令 | `calls.py` | 一对一 offer/answer/ICE 定向转发、连接绑定、超时/拒接/断开/结束落库；不记录 SDP/ICE/媒体 |
| 存储 | `work_models.py`、`work_schemas.py` | 旧分支工作模块模型与共享信封/响应；新增 CallLog |
| 前端工作台 | `PatientConsultationView.vue`（由 `ConsultationsView.vue` 双标签容器挂载） | 三态列表、历史补齐、发送中/确认/重试、图片、通话记录 |
| 前端视频 | `ConsultationVideo.vue` | 媒体权限、RTCPeerConnection、ICE 缓冲、接听/拒接/挂断、摄像头资源释放 |
| 前端记录 | `components/ConsultationRecords.vue` | 应用筛选快照、列表三态、分页、导出匹配全集、跳转完整会话 |
| 启动 | `scripts/dev.*` → `backend/start.*` | 依赖、配置、Redis、迁移/种子、热重载；Windows 隐藏子进程并写 runtime 日志 |
| 手测身份 | `app/dev_session.py` | 本地 CLI 为四个演示账号签发真实短期 JWT，无新增 HTTP 登录旁路 |

## 数据流与边界

1. HTTP 请求经过默认鉴权，数据访问统一落到 `patient_scope/visible_patient`。跨科室记录查询结果为空，单对象访问越范围返回 404。Waiting 共享队列按患者范围可见；Active/Ended 仅发起端与接诊医生可访问，其他人 HTTP 返回 404、WebSocket 拒绝连接；对可见 Waiting 会话执行需要参与身份的操作仍返回 403。
2. 创建问诊的工作人员充当患者协助端；接诊医生是另一个参与者。没有患者客户端。部门内其他有范围的医生和管理员可以只读查阅问诊记录，不能因此发送消息或加入视频。
3. 图片经内容解码校验、5 MiB 入站上限、25MP 像素上限、重新编码、随机文件名后落盘。未绑定图片仅上传者可读；绑定会话后按患者数据范围只读访问。浏览器携带 Bearer 取 blob，不提供匿名文件下载。
4. 消息写入 `consult_message`，更新列表预览，提交事务后广播持久化 ID。`(consultation_id, sender_id, client_id)` 唯一键支持丢确认后的重试；相同 key 不同内容返回 409。
5. 前端按服务端 ID 去重合并；`before_id` 逆序加载旧消息，`after_id` 正序补齐离线消息。使用 ID 游标可避免同一时间戳导致漏消息；这实现了重连补齐需求。
6. 记录中心与 CSV 共用 `RecordFilters.statement`，不额外扩大数据范围。关键词按字面包含匹配，用 EXISTS 避免同房间多条匹配导致重复会话。日期按 Asia/Shanghai 的创建日期区间，结束日期含整天。CSV 含 BOM、attachment 文件名，并中和以公式前缀开头的文本。
7. 视频媒体走浏览器之间的 WebRTC，不经过 FastAPI。WS 只传 SDP/ICE 等信令。呼叫绑定具体两个 WS 连接，不能被同账号其他标签或普通房间旁观者劫持；每条入站事件和出站事件仍检查 JWT/数据范围。
8. 呼叫 25 秒未接通结束；失联连接在 20 秒无入站响应后关闭（每 5 秒探测），触发结束。call_log 只存结束记录，接通到挂断时长由服务器计算；未接通时长为 0。服务正常关闭会写 server_restart；强杀/崩溃最后一条可能丢失，不能宣称可靠持久通话事件日志。
9. 当前 ChatHub、呼叫状态都在单个 API worker 内。Redis 用于认证和在线租约，不承担跨进程消息总线。生产使用多个 worker 前必须另做 Pub/Sub 和分布式状态。

## 契约变更

- `Consultation.is_participant` 只控制前端可写体验，后台独立检查。
- `/records`、`/export` 在原 `patient_no/q/from/to` 外增加可选 `patient`（姓名包含或患者号精确）。CSV 导出全部命中记录，分页列表用 total 表示同一全集。
- 新增受保护 `/uploads/{filename}` 下载契约，补实际上传错误码 413 / 422。
- `/calls` 实现已有 GET/POST 契约；可选 `call_id` 作为重试键，GET 返回 `end_reason`。POST 校验时序/时区/时长，不允许伪造未来时长；正常浏览器不调用 POST，由服务器信令结束后自动落库。
- WS 增加 `call_connected/call_end/call_reject`，以及既有 ping/pong/status/error 事件说明。握手拒绝在实际 ASGI WebSocket 升级前执行。

## 验证与待手测

已完成验证：

- 前端 `npm test`：26 项通过；`npm run build` 成功，保留原有大 chunk 警告。
- M3 专项：11 项通过，含 main/旧分支两种升级起点的数据保留和 downgrade base；历史 8 项业务回归也已通过。
- 空库 `scripts/build_db.py --db backend/runtime/m3-verification.db` 成功；`alembic check` 无差异；单 head `a9c327e80f16`，17 张表、两个审计触发器。
- Windows 开发入口 `-NoServe -SkipInstall` 与 `-SkipInstall` 实测成功；Vite 代理健康检查返回 db=ok、redis=ok。修改 Python 文件触发 Uvicorn 自动换工作进程，旧会话断开后重连。
- `scripts/verify_m3_browser.cjs`：Edge headless 两个隔离上下文、真实 API/Redis，测试摄像头/音频流双向连接成功；图文中的文字发送/持久化、挂断/记录刷新、结束只读、记录筛选/BOM CSV 和浏览器无未捕获异常通过。使用的是合成媒体，不是物理摄像头验收。
- `scripts/verify_m3_runtime.py`：真实 Uvicorn + Vite WS 代理 + Docker Redis，匿名/非参与者握手均返回 HTTP 403；20 连接同一消息持久化广播优化后 **334.1ms**。FakeRedis/TestClient 专项 **249.5ms**。此前并行负载下测过 526ms / 937ms，已据此将逐事件范围鉴权合并查询、在线租约按 20 秒续约并共享序列化结果；这些数值是本机测试，不是生产 SLA。
- OpenAPI YAML 语法通过（78 条目标路径）；PowerShell 与 Bash 启动脚本语法通过。WS 日志 Token 脱敏有回归，测试会话结束后撤销。

最终完整后端回归：**107 passed**（216.66 秒），Ruff lint 与 format check 均通过；仅保留依赖库的两条弃用警告。后端测试使用临时 SQLite 和 FakeRedis；另外运行真实开发 Redis 和启动流程检查，不能混称同一种覆盖。

当前状态为**部分完成**，不是已完成。需要用户按《开发模式启动与手测》逐项记录：真实摄像头/麦克风双向 60 秒通话、硬件权限拒绝、Excel 打开、实际断网恢复、局域网 HTTPS。M5 还没有会诊模型和参与者服务，通用 WS 路由复用仍待跨模块联调，当前整数 room_id 只表示 consultation_id。S3 模拟回放不在本次实现内。

可追溯测试：`tests/test_b_work.py` 保留历史消息/权限/图片/20 连接覆盖；`tests/test_m3.py` 覆盖记录组合筛选、日期边界、CSV 公式保护、通话记录时序/幂等、SDP 原样转发、拒接/挂断/断开/超时和结束只读。

历史医嘱对接仍需 M4 提供真实病历 loader 和校验器；缺依赖会返回 503。保留代码不表示该功能已经完成跨模块验收。其他主线模拟页面和全项目契约校验缺口也不因本次 M3 修改而自动完成。


## 2026-09-19：开发 Redis 排错与 S1 短信登录模拟

- 现场证据：D 盘运行副本提交 `56b5f9e`，API ready HTTP 200 但 Redis disabled；开发 Redis 使用 16379，而旧 API 没有加载 Redis URL。本次修改位于 C 盘工作副本，部署者需更新实际启动目录。
- 启动：Windows PowerShell 5.1 与 POSIX 共用 `app/dev_runtime.py` 端口预检和依赖就绪检查；Vite strictPort；Docker 引擎预检、Redis 等待、重启策略。Redis probe 使用完整 URL，保留认证/TLS 参数。ready 在 Redis disabled 时为 503，liveness 与普通 health 的约定不变。
- S1 数据流：密码 → Redis ticket → `sms.py` 共用 OTP 限流/摘要 → 可替换 `SmsProvider` / `MockSmsProvider` → SQLite `notify_outbox`；ticket 对应临时 Redis 消息用于 preview；verify 复用原子消费逻辑签 JWT。没有调用短信网关。
- 新迁移 `c93f105d2e71` 接在 M3 head 后；同步 `build_db.py` 表契约。`APP_ENV=dev` 才能模拟短信登录；默认 production。明文仅存在开发 outbox 与 300 秒 ticket 消息中，审计不记录消息正文；管理员列表对过期/非 dev 记录隐藏 code。
- 前端 SMS 调用真实后端，模拟标识仍可见；不再使用浏览器 SMS mock 的假成功。输入号码仅为测试目标，用户身份来自密码 ticket。发送、查看、验证均有 10 秒请求超时。
- 范围：未接入微信和 M6 提醒，不宣称 S1 全模块完成。此次修复基于已推送的 `90a68db`；fetch 发现 main 后续新增 M4/M5/M6 至 `9beffff`，本次没有混入这些其他模块的新合并。
- 验证结果见下方最终验证记录。


### 本次最终验证记录

- 后端全套：116 passed（190.55s），包括短信新增用例、既有认证并发消费、M3 问诊以及迁移升级/降级；2 项已有依赖弃用提示。
- Ruff check / format --check：通过；Alembic check：无模型漂移。
- 前端：26 tests passed，Vite production build 通过（已有大分包提示）。
- 独立建库 `backend/runtime/sms-verification.db`：head `c93f105d2e71`，18 张表（含 alembic_version）、2 个审计触发器、4 演示账号、3 患者。
- Windows PowerShell 5.1：验证旧 API 占用 8000 时明确退出；停止旧开发服务后，修复版本通过脚本启动，实测 ready 为 `db=ok, redis=ok`。
- Edge + Playwright + 真实 Redis：通过页面密码登录 → SMS (simulated) → Send code → View simulated SMS → 验证 → Dashboard；`/api/me` 为 dr_wang，浏览器无异常。验证脚本 `scripts/verify_sms_browser.cjs` 结束时注销测试 token，不输出验证码或 token。
- 首次全套测试因 Windows 沙箱临时目录权限报错；在正常用户环境重跑通过，属于环境限制。初次验证使用 C 盘副本；随后按用户要求同步并切换到 D 盘，见下方交接记录。


### 同日交接：以 D 盘原仓库为准

用户原仓库为 `D:\AAA-Finally\Doctor_Work_Platform\Doctor-Work-Platform`。确认无本地改动后，已从 `56b5f9e` 快进到功能提交 `4e36ebf`，不需要手动复制文件。C 盘测试服务已停止；此后的开发与启动以 D 盘原仓库为准。

D 盘原 `backend/doctor.db` 保留，迁移前通过 SQLite backup 备份到 `backups/before-dev-config-*.db`。检查发现原库无用户、无患者加密字段值，且无 backend/.env，因此在备份后生成私有本地配置（不入 Git），迁移至 `c93f105d2e71` 并补齐 4 演示账号、3 患者。没有用 C 盘数据库覆盖 D 盘数据库。

D 盘完成依赖安装并通过 `scripts/dev.ps1 -SkipInstall` 启动；再次实测 `ready: db=ok, redis=ok`、Edge 密码+模拟短信完整登录流程通过，进程路径确认为 D 盘。服务保留运行供手动测试。注册仍需原邮箱通道；演示账号可使用模拟短信登录。


## 视频同机测试修复

原先强制双端采集音视频，存在单摄像头争用；所有阶段共用 25 秒期限，设备授权慢时会过早超时。新增 Local media 三种模式（摄像头+麦克风 / 仅麦克风 / 仅接收）；仅接收仍走真实 WebRTC，但不打开任何设备、不生成虚假媒体。统一释放轨道，设备失败显示可操作提示。

信令新增 call_accept：60 秒等接听，点击接听后 45 秒设备准备，answer 后 45 秒媒体连接；服务端只允许受邀连接接受一次、应答一次，保留旧客户端直接 answer 的兼容性。详见《视频通话同机测试与排错》。旧记录中的 25 秒说明以此更新为准。


视频修复验证（2026-09-20）：

- 前端 29 项测试通过，生产构建通过；Ruff check / format 通过。
- M3 + 原分支业务回归首次 19 通过，20 连接性能用例在并行构建时为 755.6ms；构建完成后独立复测 245.4ms，通过原 500ms 门槛，未放宽阈值。
- `scripts/verify_video_modes.cjs`（独立无界面 Edge，合成媒体）通过：模拟 Device in use 后 receive-only 恢复；主叫/被叫分别只接收均能收到 WebRTC 视频与音频；被叫媒体授权延迟 30 秒仍能双向接通；挂断与授权等待中取消后，所有采集轨道 ended；无浏览器异常。首轮页面连接等待在热重载期间超时，服务稳定后完整复验通过。
- 自动化没有打开或验收用户物理摄像头；物理设备单向与双向测试步骤见《视频通话同机测试与排错》。
- 用户对《开发模式启动与手测》的现有未提交修改保留，未纳入本次提交。


## 2026-09-20 · D 盘与最新 main 冲突处理

从 D 盘干净的 `9c72ea8` 开始，合入 `main@c98324e`。12 个文本冲突按功能合并，保留用户的启动文档修改；没有在 C 盘编辑或复制文件。M3 工作台拆入 PatientConsultationView，main 的 M5 标签和 M4/M6 页面保留，SMS 与视频三种媒体模式保留。

发现并处理自动合并无法发现的冲突：两份 medical_order 表、健康/提醒/医嘱同名路径、重复模型类名与设置字段。旧表与 API 放入 legacy 兼容范围；两条历史建表迁移添加兼容判断，新 merge head 为 e6a31c29d408，具体原因和升级步骤见《main合并与升级说明》。没有将旧医嘱强行关联到 main 病历。

验证：完整后端 202 passed，新增合并/日志回归 4 passed；前端 46 passed；空库 36 表和 2 触发器，alembic check 无差异。浏览器在独立 SQLite 测试库和真实 Redis 上验证图文、视频/只接收/延迟授权/设备释放、记录 CSV 与模拟短信登录。测试曾因 Docker 未运行而收到 Redis 503，用户启动 Docker 后已通过。另修复原 Uvicorn 日志脱敏器破坏访问日志参数的问题，并补真实 formatter 回归。

本轮未改动现有 backend/doctor.db；临时前后端使用 8001/5174，验证后关闭。开发启动仍在 D 盘执行 scripts/dev.ps1，先备份旧库、保留原密钥，启动脚本负责原地升级。真实硬件与局域网验收尚未代替用户完成，M3 保持部分完成状态。

补充复验：M3/M5 标签切换加载真实会诊 API、保留 room 参数并重新挂载聊天室通过；修复后的服务访问日志没有格式异常。


## UI 入口与结束状态显示修正

上次合并时误将旧 Consultation Records 侧栏加回；现在按新版导航只保留 Consultations，在 Patient consultation 内通过 Search consultation records 打开记录查询/CSV。旧 /consultation-records 仅重定向，组件不再注册为独立页面。

用户反馈隔天已结束会话似乎恢复 Active。本次只读检查手测库：有结束成功审计的会话仍保持 ended 和 ended_at，没有发现持久化状态回退；同一患者存在多条不同状态的会话，原 UI 缺少会话编号，不容易区分。因此添加 Session 编号与创建/开始/结束时间，选择会话时 URL 保留 room ID。修正打开/刷新已结束房间及收到远端状态事件后列表分类不同步的问题；已收到 ended 时忽略同一房间迟到的非 ended HTTP 状态，防止页面回退。

验证：前端 46 项测试与生产构建通过；扩展浏览器回归覆盖单入口、内嵌记录、旧链接、记录打开/返回、M3/M5 切换，以及两端 Ended 列表、刷新后消息和通话历史保留。新增后端回归验证重复运行真实种子、创建新应用后仍为 ended、结束时间不变且不可重新接诊，1 passed。未修改用户既有库或将其他 Active 会话强制结束。
