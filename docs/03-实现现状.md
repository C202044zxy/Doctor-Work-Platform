# 医生工作平台 · 实现现状

> **版本**：v1.0
> **日期**：2026-09-14
> **地位**：本文是**代码事实的唯一快照**——回答"现在到底建成了什么"。任务划分、编号、负责人、排期、技术选型以《docs/01-任务安排.md》为准；测试场景与答辩口径以《docs/02-测试场景.md》为准；字段级契约以 `docs/api/openapi.yaml` 与 `docs/api/API-索引.md` 为准。**本文与代码冲突时以代码为准，并应立即修订本文。**
> **生成方式**：逐文件核对 `backend/app/**`、`backend/migrations/versions/**`、`backend/tests/**`、`frontend/src/**`，并用脚本比对契约路径集合。所有结论都带 `文件:行号`，可原样复核。
> **核对时点**：`main` 分支，2026-09-14。

---

## 1. 这份文档怎么用

- **要接某个模块**：先看 §5 确认它在契约里有几条路径、有没有实现，再看 §8 的缺口清单，避免照着契约写出一个已经存在的接口。
- **要判断某模块能不能标"已完成"**：看 §2 有没有路由、§7 有没有覆盖它的测试，两样都齐才算。
- **怀疑文档吹牛**：直接用 §9 的命令重算本文的每个数字。

**一句话结论**：契约里 77 条路径，**只有 17 条有对应实现**；已实现的 20 条路径里，**3 条与契约对不上**。前端 9 个路由，**只有 2 个接真实接口**。这不是缺陷清单，是当前基线——`docs/01-任务安排.md` §2 的状态列就是按它填的。

---

## 2. 后端已实现路由（41 条路由 / 32 条唯一路径）

全部路由在 `backend/app/main.py:205-208` 通过 `include_router` 挂载，**不带 prefix**——各模块的装饰器里已经写了完整绝对路径，所以"装饰器写的路径"就是"生效路径"。**没有任何动态注册**。

### 2.1 直接挂在 app 上（15 条，`backend/app/main.py`）

| 方法 | 路径 | 处理函数 | 位置 |
|------|------|----------|------|
| GET | `/health` | `health` | `main.py:236` |
| GET | `/api/health` | `health`（同一函数，第二个装饰器） | `main.py:237` |
| GET | `/api/health/live` | `live` | `main.py:269` |
| GET | `/api/health/ready` | `ready` | `main.py:273` |
| GET | `/api/departments` | `departments` | `main.py:301` |
| GET | `/api/patients` | `patients` | `main.py:315` |
| POST | `/api/patients` | `create_patient` | `main.py:396` |
| GET | `/api/patients/{patient_no}` | `get_patient` | `main.py:438` |
| PATCH | `/api/patients/{patient_no}` | `update_patient` | `main.py:456` |
| DELETE | `/api/patients/{patient_no}` | `delete_patient` | `main.py:492` |
| GET | `/api/allergens` | `allergens` | `main.py:512` |
| GET | `/api/patients/{patient_no}/allergies` | `patient_allergies` | `main.py:527` |
| POST | `/api/patients/{patient_no}/allergies` | `create_allergy` | `main.py:548` |
| PATCH | `/api/allergies/{allergy_id}` | `update_allergy` | `main.py:595` |
| DELETE | `/api/allergies/{allergy_id}` | `delete_allergy` | `main.py:627` |

### 2.2 认证（`backend/app/auth.py`，5 条）

| 方法 | 路径 | 处理函数 | 位置 |
|------|------|----------|------|
| POST | `/api/auth/login` | `login` | `auth.py:170` |
| POST | `/api/auth/verify-code` | `verify_code` | `auth.py:196` |
| GET | `/api/me` | `me` | `auth.py:239` |
| POST | `/api/auth/logout` | `logout` | `auth.py:244` |
| POST | `/api/auth/send-code` | `send_code` | `auth.py:252` |

### 2.3 邮箱注册（`backend/app/signup.py`，2 条）

| 方法 | 路径 | 处理函数 | 位置 |
|------|------|----------|------|
| POST | `/api/auth/signup` | `signup` | `signup.py:53` |
| POST | `/api/auth/signup/verify` | `verify_signup` | `signup.py:93` |

### 2.4 人脸登录（`backend/app/face_login.py`，1 条）

| 方法 | 路径 | 处理函数 | 位置 |
|------|------|----------|------|
| POST | `/api/auth/face/login` | `face_login` | `face_login.py:48` |

### 2.5 临时授权（`backend/app/grants.py`，3 条路由 / 2 条路径）

| 方法 | 路径 | 处理函数 | 位置 |
|------|------|----------|------|
| POST | `/api/temp-grants` | `create_grant` | `grants.py:116` |
| GET | `/api/temp-grants` | `list_grants` | `grants.py:145` |
| DELETE | `/api/temp-grants/{id}` | `revoke_grant` | `grants.py:178` |

> **注意**：`DELETE` 的路径参数名是 `{id}`，与契约一致；但患者过敏那两条用的是 `{allergy_id}`，契约写的是 `{id}`——见 §5.2。

### 2.6 会诊（`backend/app/meetings.py`，15 条路由 / 12 条路径）

| 方法 | 路径 | 处理函数 | 位置 |
|------|------|----------|------|
| GET | `/api/meetings/doctors` | `list_doctors` | `meetings.py:224` |
| GET | `/api/meetings` | `list_meetings` | `meetings.py:280` |
| POST | `/api/meetings` | `create_meeting` | `meetings.py:336` |
| GET | `/api/meetings/{id}` | `get_meeting` | `meetings.py:440` |
| POST | `/api/meetings/{id}/accept` | `accept_meeting` | `meetings.py:527` |
| POST | `/api/meetings/{id}/decline` | `decline_meeting` | `meetings.py:533` |
| POST | `/api/meetings/{id}/start` | `start_meeting` | `meetings.py:539` |
| POST | `/api/meetings/{id}/complete` | `complete_meeting` | `meetings.py:545` |
| GET | `/api/meetings/{id}/participants` | `list_meeting_participants` | `meetings.py:554` |
| GET | `/api/meetings/{id}/materials` | `list_meeting_materials` | `meetings.py:573` |
| POST | `/api/meetings/{id}/materials` | `upload_meeting_material` | `meetings.py:587` |
| GET | `/api/materials/{id}/download` | `download_material` | `meetings.py:634` |
| GET | `/api/meetings/{id}/report` | `get_meeting_report` | `meetings.py:674` |
| POST | `/api/meetings/{id}/report` | `save_meeting_report` | `meetings.py:699` |
| GET | `/api/meetings/{id}/report/print` | `print_meeting_report` | `meetings.py:752` |

> **`/api/meetings/doctors` 是契约之外的一条**。契约里可用的用户目录是 M1-07 的 `/api/users`，而它只对管理员开放；M5-01 的发起人可以是 `junior`，选择专家的下拉框读不了它。这条路由只返回 `id/username/name/title/department`，**不含邮箱**，M1-07 落地后应当删除。
>
> **资料上传与下载共用 `backend/app/uploads.py`**（与 M3-02 同一组件：同一大小上限 10 MiB、同一随机落盘名、同一按内容类型校验、同一审计钩子），**只有白名单不同**——这里接受 PDF / 图片 / 文档，比图片宽。下载响应带 `Content-Disposition`，原名同时写在 `filename`（非 ASCII 降级为 `_`，HTTP 头是 latin-1）与 RFC 5987 的 `filename*` 里，所以中文名到客户端仍可读。

### 2.7 匿名白名单（10 条，不需要 token）

`backend/app/security.py:25-38` 的 `ANONYMOUS_PATHS` 是一个 frozenset，恰好这 10 条：

`/health`、`/api/health`、`/api/health/live`、`/api/health/ready`、`/api/auth/login`、`/api/auth/send-code`、`/api/auth/verify-code`、`/api/auth/signup`、`/api/auth/signup/verify`、`/api/auth/face/login`。

其余全部路由由 `ProtectedFastAPI.include_router`（`security.py:75-84`）**默认注入 `current_user` 依赖**——即"默认受保护，白名单才放行"，不是逐个路由手写鉴权。

> ⚠️ `/api/auth/face/login` 在这个白名单里，是 §8.1 那条红线的直接来源。

### 2.8 没有 WebSocket

全仓库 `backend/app/` 检索 `websocket` **零命中**。契约里的 `/ws/chat/{room_id}`（`openapi.yaml:2863`）**没有任何实现**，问诊与会诊的实时能力为零。

---

## 3. 数据模型（`backend/app/models.py`）

8 张表，`Base` 定义在 `models.py:17`。

| 类 | 表名 | 位置 | 关键列与约束 |
|----|------|------|--------------|
| `Department` | `departments` | `:21` | `id` PK；`name` String(100) unique |
| `Role` | `roles` | `:27` | `id` PK；`name` String(50) unique |
| `User` | `users` | `:33` | `username` unique；`name`；`status`（默认 `active`）；`created_at`；`email` unique；`password_hash`；`role_id` FK；`department_id` FK；`role` / `department` 均 `lazy="joined"` |
| `Patient` | `patients` | `:50` | `patient_no` String(20) unique+index（**nullable**，见 `:53-55` 注释）；`name` index；`gender`（默认 `unknown`）；`birth_date`；`department_id` FK；`notes`；`phone_enc`；`id_card_enc`；`symptom_tags` String(500)；`admitted_at`；`created_at`；`deleted_at`（软删） |
| `Allergy` | `allergies` | `:76` | `patient_id` FK **ondelete="CASCADE"** index；`allergen` String(50)；`allergy_type`；`severity`；`reaction`；`recorded_at`（必填） |
| `AuditLog` | `audit_logs` | `:91` | `action`；`patient_id`（无 FK）；`user_id`；`ip`；`method`；`path`；`object_type`；`object_id`；`result`；`status_code`；`detail` JSON；`created_at` |
| `TempGrant` | `temp_grant` | `:111` | `grantee_id` FK index；`patient_id` FK index；`reason`；`granted_by` FK；`expire_at` index；`is_valid`（默认 True）index；`created_at` |
| `Passkey` | `passkeys` | `:127` | `user_id` FK index；`credential_id`；`credential_hash` unique；`public_key`；`sign_count` —— **死表，见 §8.3** |

**两点值得注意**：

1. **`symptom_tags` 是 `String(500)` 的拼接串，不是结构化表**。契约与任务书里"结构化以支持搜索"的表述，现状是"逗号拼接 + `LIKE` 匹配"（`schemas.py` 的 `join_tags` / 拆分函数）。M2 收口时若要真正的结构化搜索，这里要动迁移。
2. `temp_grant` 的列名严格是 `expire_at` / `is_valid`，**没有** `expires_at` / `status` / `target_type` / `duration_hours`——与契约一致。

---

## 4. 迁移链（11 个版本，M4 / M6 分支汇合）

`backend/migrations/versions/`，当前 head = `b4c6d0192026`。

| 文件 | revision | down_revision | 做什么 |
|------|----------|---------------|--------|
| `b75f06934276_initial_foundation_schema.py:6` | `b75f06934276` | `None` | 建 `audit_logs` / `departments` / `roles` / `patients` / `users` / `allergies`（含 `ON DELETE CASCADE`） |
| `9f2c7b41d5ae_patient_profile_fields.py:22` | `9f2c7b41d5ae` | `b75f06934276` | 加 `patients.patient_no/phone_enc/id_card_enc/symptom_tags/admitted_at/created_at`；回填 `patient_no` 为 `P<年><序号>`；建唯一索引 `ix_patients_patient_no` |
| `c1d4e7a90b52_contract_alignment.py:22` | `c1d4e7a90b52` | `9f2c7b41d5ae` | 加 `patients.gender/birth_date/deleted_at`；过敏表补 `allergen/allergy_type/severity/reaction/recorded_at`，由 `substance` 回填后**删列**；`audit_logs` 加 `detail` |
| `d2a5f8b13c64_sprint1_auth_grants.py:6` | `d2a5f8b13c64` | `c1d4e7a90b52` | 加 `users.username/name/status/created_at`；**把角色名 `administrator`/`department_manager`/`doctor` 改成 `admin`/`senior`/`junior`**；建 `temp_grant` |
| `e3b6c9d24f75_passkeys.py:6` | `e3b6c9d24f75` | `d2a5f8b13c64` | 建 `passkeys`（**应用代码从不读写**） |
| `c0311060809b_security_foundation.py:6` | `c0311060809b` | `e3b6c9d24f75` | `audit_logs` 补 `user_id/ip/method/path/object_type/object_id/result/status_code`，`patient_id` 改可空；**建 append-only 触发器** `audit_logs_no_update` / `audit_logs_no_delete`，SQLite 用 `RAISE ABORT`、MySQL 用 `SIGNAL SQLSTATE 45000` |
| `786395e6a0ba` | `786395e6a0ba` | `c0311060809b` | 审计操作者名称快照与查询索引 |
| `f4b7c1e93a20_m5_remote_consultation.py:6` | `f4b7c1e93a20` | `786395e6a0ba` | 建 `meetings` / `meeting_participants`（唯一键 `uq_meeting_participant`）/ `meeting_materials` / `meeting_reports`（唯一键 `uq_meeting_report_version`）。**刻意不用 `batch_alter_table`**：SQLite 下它会重建被改的表，并静默丢掉 `audit_logs` 的两个 append-only 触发器 |

| `7ad4cb527bd2_add_m4_records_prescriptions_and_rules.py` | `7ad4cb527bd2` | `f4b7c1e93a20` | M4 模板、病历、版本、药品规则与医嘱五张表 |

| `a3d9e5f21c70` | `a3d9e5f21c70` | `f4b7c1e93a20` | M6 体征、健康方案、提醒与评估六张表 |
| `b4c6d0192026_merge_m4_m6.py` | `b4c6d0192026` | `7ad4cb527bd2`, `a3d9e5f21c70` | 汇合 M4 与 M6，保留已发布迁移历史 |

> 角色改名发生在迁移里，说明 `admin`/`senior`/`junior` 口径**已经落库**，不再只是种子数据里的字符串。

---

## 5. 契约 ↔ 实现差距

用脚本把 `docs/api/openapi.yaml` 的 `paths` 与上表 20 条路径做集合比对（`§9` 有命令）。

### 5.1 总数

| 项 | 数量 |
|----|------|
| 契约声明的路径 | **77** |
| 后端实现的唯一路径 | **61** |
| **精确匹配** | **53** |
| 实现了但契约里没有 | **8** |
| 契约里有但没实现 | **24** |

### 5.2 八条对不上的（`M0-05` 契约校验将来必须拦住的就是这些）

| 路径 | 问题 |
|------|------|
| `/api/allergies/{allergy_id}` | **参数名与契约不一致**。契约（`openapi.yaml:822`）写的是 `/api/allergies/{id}`。按路径字符串严格比对，这是一条"多出来的接口 + 一条没实现的接口"，两条误报 |
| `/api/health/live` | **契约缺失**。代码有（`main.py:269`）、CI 有依赖、`ANONYMOUS_PATHS` 有它，但契约里没有 |
| `/api/health/ready` | **契约缺失**。同上（`main.py:273`），且它的响应不是纯 `{code,message,data}` 信封——见 §5.3 |
| `/api/meetings/doctors` | **契约缺失**。M5 新增（§2.6）。契约里管理员专属的 `/api/users` 喂不了 junior 发起的专家选择框；M1-07 落地后删除 |

M6 还存在四条路径参数名差异：`/api/assessments/{assessment_id}`、`/api/health-plans/{plan_id}`、`/api/reminder-rules/{rule_id}`、`/api/reminders/{reminder_id}/done`；契约对应参数均为 `{id}`。本表采用精确路径匹配，未将这些差异当成相同路径。

### 5.3 健康检查的真实行为（三处口径不一致，务必分清）

| 路由 | 行为 |
|------|------|
| `GET /health` 与 `GET /api/health` | **恒 200**。依赖 down 只写在 body 里：`{"db": "ok"\|"down"\|"disabled", "redis": ...}`。键名是 **`db`**，不是 `database`（`main.py:236-254`，理由写在 `:239-241`：`auth_schemas.py` 由契约生成） |
| `GET /api/health/live` | 恒 200，普通信封（`main.py:269-271`） |
| `GET /api/health/ready` | **依赖 down 时返回 503**：`available = "down" not in checks.values()`，`status_code=200 if available else 503`（`main.py:289-291`）。响应体还额外带顶层 `status` 与 `checks`，其中 `checks` 与 `data` 内容重复——**这不是纯信封** |

**因此"健康检查永不出 5xx"这句话只对前两条成立。** 全站确实存在 503：`/api/health/ready`、`RedisError`（`main.py:230-234`）、`SQLAlchemyError`（`main.py:256-261`）、以及 `JWT_SECRET` 未配置（`backend/tests/test_security.py::test_unconfigured_jwt_secret_fails_closed` 断言了 503）。任何声称"本 API 没有 503"的文档都是错的。

### 5.4 契约里没有实现的 24 条，按模块归属

> 2026-09-19 合并 M4 与 M6 后重新统计，见 §5.1。

`/api/users`、`/api/users/{id}`、`/api/roles`（M1-07，3 条）；`/api/patients/{no}/histories`、`/api/histories/{id}`、`/api/patient-groups*`（M2，5 条）；`/api/consultations*`、`/api/uploads/images`、`/ws/chat/{room_id}`（M3，10 条）；`/api/audit-logs/{id}`（M8 单条详情——T12 明确不做，列表行内已带 `detail`）。

**结论**：契约写得比实现早得多，它描述的是目标态。**不要拿契约当"已实现"的证据。**

---

## 6. 前端现状

**9 个业务路由**（下表），路由表在 `frontend/src/router/index.js:24-120`；侧边栏由同一份 `navigation` 数组渲染（`:15-22`），所以菜单与路由不会漂移。另有 `/403`、`/face/enroll` 与未知路径的兜底重定向，不属于业务页；`/remote-consultation` 也不是业务页，见下。

| 路由 | 视图 | 数据来源 | 状态 |
|------|------|----------|------|
| `/login` | `LoginView.vue` | `api/client.js`（真实） | ✅ 接真实接口 |
| `/patients` | `PatientListView.vue` | `api/client.js`（真实） | ✅ 接真实接口；名称列进患者详情 |
| `/patients/:patientNo` | `PatientDetailView.vue` | `api/client.js`（真实） | ✅ 接真实接口（**「会诊记录」Tab** 属 M5、**「Health data」Tab** 属 M6，其余 Tab 属 M2-04） |
| `/dashboard` | `DashboardView.vue` | `GET /api/me`（会话里已有）/ `/api/meetings` / `/api/patients` / `/api/reminders?unread_only=true` / `/api/reminders/unread-count` | ✅ 真实接口（2026-09-19） |
| `/records` | `MedicalRecordView.vue` | `api/client.js` | ✅ 模板、病历版本与医嘱 |
| `/consultations` | `ConsultationsView.vue` | 见下 | ⚠️ **两个 Tab 各自不同**：「Remote consultation」接真实接口（M5），「Patient consultation」是一个写明"M3 未做"的空态，**没有任何假数据** |
| `/review` / `/my-submissions` | `ReviewQueueView.vue` | `api/client.js` | ✅ senior 待审队列 / 本人提交与退回意见 |
| `/audit` | `AuditLogView.vue` | `api/client.js`（真实） | ✅ 接真实接口（查询 / 筛选 / 分页 / 服务端 CSV 导出，T12） |

**合并后 View 与 App.vue 均无 demo-data.js 直接导入。** M4 两页接真实病历接口；工作台、外壳与 M6 患者健康面板保留 main 的真实接口实现。M3 患者问诊仍为空态，未实现。

**两个模块共用一个侧边栏入口。** `0915意见` 第 5 条要求问诊页"点进去之后两个模块"：`/consultations` 现在是一个容器（`ConsultationsView.vue`），用 Tab 在「Patient consultation」（M3，零代码）与「Remote consultation」（M5，已交付）之间切换，选中的 Tab 记在 query（`?tab=remote`）里所以能直接链到。M5 的页面作为子组件**原样挂载**，没有改动它一个字符；M5 原来的 `/remote-consultation` 改成重定向到 `?tab=remote`，旧链接（书签、场景表、幻灯片里的截图）仍然落在正确的位置。

**M3 那一半是一个空态，不是一个做得像的空壳。** 原来的 `ConsultationsView.vue` 是一整套仿微信工作台，喂的是 `demo-data.js` 的 `consultSessions` / `consultMessages` / `consultPatient`，而且页脚写着 **"Connected over WebSocket"** —— 而全仓库没有任何 WebSocket 路由。那份假数据已经删除（工作台的布局草图留在 `git show 65eaf88`），M3 落地时填的是「Patient consultation」这一个 `el-tab-pane`。

**健康管理不再有独立入口。** `HealthManagementView.vue` 与 `/health` 路由已于 2026-09-18 删除（`0915意见` 第 3 条：健康管理与患者管理合并）。面板本身（`components/PatientHealth.vue`）从一开始就是患者维度的，挂在患者详情页的「Health data」Tab 即可；再留一个侧边栏入口，等于同一份内容两个门，且这两个门会各自漂移。

**路由守卫现在有角色门禁**（`router/index.js:145-160`）：先判是否登录，再对 `meta.roles` 跑 `canOpen()`（`access.js`），不放行就转 `/403?from=…`；侧边栏的 `navigation` 也按同一份 `MODULE_ROLES` 过滤，所以 `junior` 既看不到"病历审阅""审计日志"的入口，直输地址也进不去。`router/index.js:143` 的注释仍写明了这是体验层、不是安全边界——真正的检查在 API 的 403 / 404。（`docs/01-任务安排.md` §2 的 M1 行仍写"前端路由角色门禁未做"：那行按 **`main` 分支**判定，而门禁目前只存在于 T07/T12 分支上，合并后同步即可。）

**真实接口调用点**：`api/client.js:10`（通用 `fetch` 封装）、`api/client.js:41`（`/health/ready`）、`App.vue:22`、`router/index.js:4`、`session.js:2`、`LoginView.vue:6`、`PatientListView.vue:5`、`PatientDetailView.vue:6`、`RemoteConsultationView.vue:10`、`components/PatientHealth.vue:18`（M6）、`views/AuditLogView.vue:5`（T12）、`views/FaceEnrollView.vue:12`。技术上没有 axios、没有 Pinia。

---

## 7. 测试现状

10 个测试文件，**94 个测试函数**（`grep '^def test_'`）。多个带 `@pytest.mark.parametrize`，实际收集 110 个用例。`tests/test_meetings.py`（M5，17 个用例）**实测全过**（2026-09-17）；**2026-09-18 本机全量 110 passed**。

> ⚠️ 2026-09-14 记录的 **76 passed** 早于 M5，重跑请以 §9 的命令为准。当时另有 **10 个既有失败**（`test_audit_logs.py` 9 个 + `test_api.py::test_department_filter_stacks_with_the_other_conditions`），根因是它们硬编码了**已作废**的科室名 `General Medicine`。《01-任务安排.md》§9 只认 Information Technology / Cardiology / Neurology，**2026-09-18 已把用例改用基线科室名（`Information Technology`、`Neurology`、`Neuro` 前缀），10 个失败清零**。

| 文件 | 测试函数 | 覆盖什么 |
|------|----------|----------|
| `test_api.py` | 26 | 患者 / 过敏 / 科室契约：信封、生命周期、患者号生成、四条件 AND 搜索、日期区间校验、排序、落库加密、脱敏、更新、软删、分页、Swagger 字段覆盖、过敏原字典与共享读取函数 |
| `test_auth_grants.py` | 10 | 登录握手 / 登出 / disabled 账号、密码限速、验证码过期与 ticket 绑定、授权可见性 / 撤销 / 到期、授权权限、无效 token + Redis 故障 + health 别名、数据范围、并发换码 |
| `test_audit_logs.py` | 10 | 审计查询与 CSV 导出：仅管理员可读、各过滤条件逐条收窄、时间区间真实绑定、分页合计、导出与列表一致、导出文件名带区间、导出自身留痕 |
| `test_security.py` | 9 | 权限矩阵 15 组合、实时库上的范围变更、发码并发、日限额、审计元数据与失败隔离、**DB append-only 断言**、默认保护与统一错误、SMTP 超时 / TLS / 脱敏、JWT_SECRET 未配置时 fail-closed |
| `test_signup.py` | 8 | 注册 → 激活 → 登录、校验、限流与过期、错码、重复与投递失败、ticket 不能直接换 token |
| `test_face_login.py` | 5 | 会话 / 登出、匹配器收到照片、账号不可用、非法图片、缺字段与已移除的 passkey 端点 |
| `test_email_delivery.py` | 3 | SMTP 投递、日限额、465 端口隐式 TLS |
| `test_auth_migration.py` | 1 | Alembic 升级保留既有用户身份与角色 |
| `test_meetings.py` | 17 | M5：状态机与非法跳转 409、发起即建 `temp_grant`、资料上传/下载（白名单更宽、中文名不乱码）、报告版本不覆盖、参与者权限 401/403/404 与 `?patient_no=` 取回 |
| `test_seed_demo.py` | 5 | 演示基线：幂等落库、漂移只报告不回写、缺失过敏补齐、`--check` 只读且非零退出、`--fix` 只回写基线字段且不删行 |

`conftest.py` 只有 fixture：autouse 地设置 `JWT_SECRET` / `PATIENT_DATA_KEY` 环境变量。

**测试的真实边界**：每个测试迁移一个**临时 SQLite**，Redis 用 `fakeredis` 替身。**所以测试既不覆盖真实 Redis，也不覆盖真实 MySQL**——CI 另有步骤覆盖 SQLite/Redis 的真实行为（见 `.github/workflows/ci.yml`）。

---

## 8. 模拟、死代码与已知缺口

### 8.1 🔴 人脸登录是模拟（最高优先级红线）

- `backend/app/face_login.py:18-21` 的 `match_face()` **恒返回 `True`**，源码自述 `# Demo only: every matching attempt succeeds until matching is implemented.`，其上一行是 `# TODO: Implement face matching against the user's enrolled reference image.`
- 该路由在匿名白名单里（`security.py:25-38`；`security.py:20-24` 有 WARNING 注释）。
- **净效果：任意合法 JPEG + 任一存在的活跃用户名 = 一个真实的 2 小时 token。**
- 契约 `openapi.yaml:92` 的描述是诚实的（"Demo matcher always returns true"），README 的说明也诚实。
- **要求**：页面可见角标、响应体 `"mock": true`、答辩口径三处一致标注为模拟；**绝不可对外部署**。

### 8.2 已移除但仍有痕迹的 passkey

`models.py:127` 的 `Passkey` 表与迁移 `e3b6c9d24f75` 都还在，但**全仓库没有任何代码读写它**（除模型定义与迁移本身）。端点已删除。README 说明是"为迁移兼容保留"。**它是一张死表**——留着不影响功能，但会让人误以为有 passkey 能力。

### 8.3 缺口清单（与 `docs/01-任务安排.md` §2 的状态列一一对应）

| 缺口 | 位置 | 对应任务 |
|------|------|----------|
| 契约校验没进 CI，仓库也没有比对脚本 | `.github/workflows/ci.yml` | **M0-05** |
| 用户管理接口不存在 | 契约有 `/api/users`、`/api/roles`，无实现 | **M1-07** |
| 前端无角色门禁 | `router/index.js:90-98` | M1（完成标准） |
| View 与 App.vue 无 demo-data.js 直接导入 | 见 §6；M3 仍未实现 | **M9-05** 的假数据清理已落地 |
| 审计查询与导出不存在 | 契约有 `/api/audit-logs*` 3 条 | **M8-02** |
| 无 WebSocket | 全仓库零命中 | M3-01 |
| 患者详情（除 M5 的「会诊记录」Tab）/ 分组 / 病史不存在 | 契约有，无实现 | M2-04 / M2-05 / M2-06 |
| `symptom_tags` 未结构化 | `models.py` String(500) | M2-06 |

### 8.4 遗留的旧编号（代码注释，未清理）

`frontend/src/router/index.js:89` 的注释引用 `T08` / `T09`。按 `AGENTS.md` 的口径，旧编号只允许出现在 `docs/01-任务安排.md` §7——**这些是待清理的残留**，属注释文本，不影响行为。

### 8.5 未删除的旧文档（1 份）

仓库根的 **`新任务安排.md`** 是重构前的文档，**仍在磁盘上**。它把 M0 / M1 / M2 标为"已完成"、把 WebSocket 与视频通话写成已交付、科室名用中文——**每一条都与本文的实测结果冲突**。2026-09-14 清理旧文档时，删除这一份的操作被权限校验拦下，因此它留了下来。

清理方式：`git rm 新任务安排.md`。在它被删掉之前，**任何读到它的人都会得到一份错的现状图**，所以 `docs/README.md`、`CLAUDE.md`、`AGENTS.md` 与 `docs/01` 都在开头显式标注了它。

---

## 9. 怎么复核本文

```bash
# 1. 契约路径 vs 实现路径（本文 §5 的数字）
cd backend
uv run --with pyyaml python - <<'PY'
import pathlib, yaml
spec = yaml.safe_load(pathlib.Path("../docs/api/openapi.yaml").read_text(encoding="utf-8"))
print(len(spec["paths"]), "contract paths")
PY

# 2. 测试数量与结果（本文 §7）
cd backend && uv run pytest -q

# 3. 迁移链与 head（本文 §4）
cd backend && uv run alembic heads && uv run alembic history

# 4. 假数据 import 点（本文 §6）
#    在仓库根执行，应命中 6 个 import 点（5 个 View + App.vue）
#    rg -l "from '.*demo-data'" frontend/src

# 5. 确认没有 WebSocket（本文 §2.8）
#    在仓库根执行，应零命中
#    grep -rn "websocket" backend/app
```

---

*本文由代码核对生成，2026-09-14；M5 段（§2.6 / §4 / §5 / §6 / §7）核对于 2026-09-17。任务与编号见《docs/01-任务安排.md》，测试场景见《docs/02-测试场景.md》，字段契约见 `docs/api/`。*


## M4 验收更新（2026-09-19）

M4-01–M4-07 的后端、数据库迁移和前端均已实现。病历与审阅页面不再导入 demo-data。

- 模板：两份至少六字段的预置模板；admin 维护；历史按模板快照渲染。
- 病历：创建、筛选分页、草稿保存、提交全量版本、退回重提、归档只读、作者补记。
- 并发：新增必填 `revision`，每次变更 compare-and-swap；业务版本 `version` 仍仅提交／补记递增。
- 医嘱：共用患者过敏源、数据库配置剂量规则、单位换算与每日频次、批量原子拦截、修改／软停及审计。
- 前端：英文动态表单、校验原文弹窗、黄色二次确认、红色无强制开立、按账号／病历隔离的 sessionStorage 草稿。
- 契约：预检始终返回 200 与 verdict；过敏冲突的保存／修改返回 409 与校验详情。`/archive` 是 pending 审阅通过的别名。

M4 专项验证：`uv run pytest -q tests/test_emr.py` **17 passed**；六格矩阵 **6/6**。浏览器脚本通过断网／刷新恢复、双标签页冲突、红黄医嘱弹窗、提交／审阅／归档／补记。`npm test` **30 passed**；`npm run build` 通过（原有包体积提示）。Ruff 检查与格式检查通过。`scripts/build_db.py --db /tmp/m4-acceptance.db` 从空库构建成功（18 表、2 个审计只追加触发器）；新增迁移回滚再升级与 `alembic check` 均通过。

浏览器使用真实 HTTP API、迁移后的 SQLite 演示库与 Redis 测试替身；此验证不代替真实 Redis 的 CI 覆盖。浏览器脚本需在隔离测试栈运行，令牌 JSON 仅保存在临时目录：`uv run scripts/check_emr_browser.py --base-url http://127.0.0.1:5173 --tokens /tmp/test-tokens.json`。

全量后端回归首次 126 passed / 1 failed（既有邮箱测试的 FakeRedis TTL 偶发读为 302 秒），该文件单独复跑 3 passed；最终全量复跑 **127 passed**（283.29 秒），仅有依赖弃用提示。

### M4 / M6 合并验证（2026-09-19）

将 `main` 的 M6 与工作台改动合入 M4 分支，同时保留两套接口、M6 的患者健康面板、会诊 Tab 和 M4「我的提交」入口。新增合并迁移 `b4c6d0192026`，保留两条已发布 revision，不重写迁移历史。`test_m4_m6_migration.py` 的两个用例分别从 M4 与 M6 原 head 升级，验证原数据保留、完整模型表集合和审计触发器。

合并后空库构建通过：24 表、2 个审计只追加触发器；`alembic check` 无差异。前端构建通过，40 项前端测试通过；Ruff 检查和格式检查通过。

后端全量回归 158 passed（342.24 秒）；随后新增的两条迁移分支升级测试单独运行 2 passed。无功能测试失败，仅既有依赖弃用提示。
